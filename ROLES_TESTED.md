# Roles Tested

Real-host `crystal-ansible`-vs-`ansible-playbook` correctness rounds run so
far, and each role's **current** status (not what was fixed to get there —
see `KNOWN_MISSING.md` and `git log` for that history). Check this before
picking a shortlist for the next round, to avoid re-discovering Galaxy-404s
or already-clean roles as if they were new.

| Role | Status |
|---|---|
| Oefenweb.swapfile | ✅ Clean (round 150, 0.9.476). Byte-identical `ok=7 changed=6 skipped=2` cold, `ok=5 changed=0 skipped=4` idempotent warm rerun, real swap verified active and identical (`swapon --show`: `/swap file 1024M 0B -2`) on both engines. |
| andrewrothstein.kubectl | ✅ Clean (round 150, 0.9.476). Byte-identical `ok=4 changed=2 skipped=1` cold (downloads and symlinks a real `kubectl` binary), `ok=4 changed=0` idempotent warm rerun on both. |
| andrewrothstein.bash | ✅ Clean (round 150, 0.9.476). Byte-identical `ok=3 changed=0 skipped=1` cold and warm on both (host already had everything the role wants). |
| andrewrothstein.sudoers | ✅ Clean (round 150, 0.9.476). Byte-identical `ok=5 changed=3 skipped=6` cold, `ok=5 changed=0` idempotent warm rerun on both. |
| mrlesmithjr.chrony | ✅ Clean (round 150, 0.9.476). Byte-identical `ok=5 changed=3 skipped=1` cold, `ok=4 changed=0` idempotent warm rerun on both (real `chrony` restart handler fired on both). |
| willshersystems.sshd | ✅ Fixed (round 150, 0.9.476). 1 real engine bug found and fixed: `conditional_evaluator.cr`'s hand-rolled array-literal parser turned an empty `[]` into a bogus 1-element array (`"".split(',')` returns `[""]` in Crystal, not `[]`), so the role's own `when: sshd_trusted_user_ca_keys_list != []` guard (default `[]`) always evaluated true and ran a certificate-copy block whose `dest:` variable came out empty, crashing the task outright ("renaming file: -> '': No such file or directory"). Fixed with an explicit empty-string guard (the sibling `{{ }}`-side `parse_literal_array` in `expression_evaluator.cr` already had this check - one remaining unguarded copy). Live-reverified: byte-identical `ok=28 changed=0 skipped=14` cold+warm vs real `ansible-playbook`, fully idempotent, after the fix. |
| buluma.python_pip | ✅ Clean (round 150, 0.9.476 - zero crystal-ansible bugs). A first cold-run apt 404 was a stale apt-cache artifact on that specific host (no `apt-get update` had run yet), confirmed by reproducing after a manual `apt-get update`; byte-identical `ok=7 changed=2 skipped=7` cold (post-update) and `ok=7 changed=0` idempotent warm rerun on both. |
| buluma.openssl | ✅ Clean (round 150, 0.9.476 - zero crystal-ansible bugs). Byte-identical `ok=14 changed=2 skipped=2` cold, `ok=14 changed=0` idempotent warm rerun on both; one cosmetic-only gap noted (crystal-ansible's recap has no `ignored=` counter for an `ignore_errors:`-caught task, though the ignore behavior itself - failed task, execution continues, final `failed=0` - matches exactly). |
| buluma.cron | ✅ Clean (round 150, 0.9.476 - zero crystal-ansible bugs). Byte-identical `ok=10 changed=4 skipped=6` cold (real `cron` restart handler fired), `ok=9 changed=0` idempotent warm rerun on both. |
| buluma.bootstrap | ✅ Clean (round 150, 0.9.476 - zero crystal-ansible bugs). Byte-identical `ok=14 changed=1 skipped=10` cold, `ok=14 changed=0` idempotent warm rerun on both. First round targeting fresh 2025/2026-updated Galaxy roles outside the robertdebock/geerlingguy catalogs already exhausted by prior rounds (buluma is an actively-maintained, robertdebock-style role family; also picked up willshersystems/andrewrothstein/mrlesmithjr/Oefenweb along the way). |
| robertdebock.cve_2018_19788 | ✅ Clean (round 139, 0.9.447). 1 real bug found and fixed: `safe_yaml_to_string`/`stringify_value` (used for EVERY module parameter's YAML->String conversion) both did `yaml.as_i.to_s` - `YAML::Any#as_i` is Int32-only, raising "Arithmetic overflow" for anything outside that range, silently dropping the WHOLE task at parse time. The role's own "Create user" task (`uid: 2147483659`, one past Int32::MAX - a completely ordinary real uid) vanished entirely, so its actual test (a non-privileged user shouldn't be able to manage a systemd service) never ran against a real user - a coincidental `failed_when: no` plus a since-undefined register made the whole run look deceptively clean instead of visibly broken. Same root-cause class as round 70's diskspace fixes, a 3rd/4th independent copy - also fixed in `plugins/debug.cr` and `CrinjaRenderer#json_any_to_crinja_value` (a large int shown via `debug:` or a real `.j2` template would hit the identical crash). Live-reverified: `ok=4 changed=1 skipped=0` matching real ansible-playbook exactly, user genuinely created, "Interactive authentication required" correctly blocking the unprivileged user from managing the service on both engines, fully idempotent warm rerun. |
| robertdebock.cve_2021_44228 | ✅ Clean (round 139, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=20 changed=0 skipped=2` on both. |
| robertdebock.debug | ✅ Clean (round 139, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=3 changed=0 skipped=1` on both. |
| robertdebock.core_dependencies | ✅ Clean (round 139, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=4 changed=1` cold (exercises `meta: flush_handlers` triggering a "Gather facts" re-gather handler), `ok=3 changed=0` idempotent warm rerun on both. |
| robertdebock.microsoft_repository_keys | ✅ Clean (round 139, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=7 changed=3` cold (the role's own RHEL-only `rpm_key:` branch hits the pre-existing, already-documented gap - zero functional impact on Ubuntu), `ok=7 changed=0` idempotent warm rerun on both, keyring files byte-identical. |
| robertdebock.travis | ✅ Clean (round 139, 0.9.448). 1 real bug found and fixed: `community.general.gem` (a real, working plugin - `plugins/gem.cr`) was registered in `AVAILABLE_PLUGINS` under the WRONG FQCN, `ansible.builtin.gem` - not a real Ansible module name at all, gem has always lived in `community.general`, never ansible-core. A bare `gem:` task still happened to resolve via the fallback search order regardless, but the role's own fully-qualified `community.general.gem:` form (the far more common style in real playbooks) got "Plugin not available" and the whole task silently dropped. Fixed the registration. Live-reverified: gem installed identically (`travis (1.11.1)`) on both hosts, `ok=3 changed=1` matching real ansible-playbook (a `changed=2` on the very first attempt was self-inflicted contamination from an earlier partial run on the same host, not a real divergence), fully idempotent warm rerun. |
| robertdebock.dryrun | ✅ Clean (round 138, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=4 changed=0` on both. |
| robertdebock.apt_autostart | ✅ Clean (round 140, no version bump). Byte-identical `ok=3 changed=1 skipped=1` cold, `changed=0` warm, both engines. |
| robertdebock.update_package_cache | ✅ Clean (round 140, no version bump). Byte-identical `ok=2 changed=1` cold and warm (module has no idempotency of its own), both engines. |
| robertdebock.buildtools | ✅ Clean (round 140, no version bump). Byte-identical `ok=2 changed=1` cold, `changed=0` warm, both engines. |
| robertdebock.zabbix_repository | ✅ Clean (round 140, no version bump). Byte-identical `ok=8 changed=3` cold, `changed=0` warm; `skipped` count differs (7 vs 5) due to the already-documented `rpm_key`/`zypper_repository` unimplemented-plugin gap (rounds 62/101/106/130), not a new issue. |
| robertdebock.filesystem | ✅ Clean (round 140, no version bump). `community.general.filesystem` against a real `/dev/loop8` ext4 format - byte-identical `ok=9 changed=3 skipped=5` cold, fully idempotent warm rerun (`changed=0`); confirms the round-135 `filesystem.cr` plugin against a real block device. |
| robertdebock.mount_options | ⚠️ Divergence found, documented not fixed (round 140, no version bump). Test setup (loop-device mount in the same play's `pre_tasks:`, before `ansible_mounts` was refreshed) hits an empty-sequence `\| first` on both engines - real Ansible hard-fails immediately with a clean "No first item, sequence was empty" task-arg error; crystal-ansible's `first` filter silently returns nil on an empty array instead of raising, so a corrupted `opts: ",nodev"` value flows through and the run fails later, in a different task, with an unrelated message. Same class as the deferred strict-undefined design gap (rounds 52/62/93/101/129) - not fixed in isolation for the same reason. See `KNOWN_MISSING.md`. |
| robertdebock.ansible_lint | ✅ Clean (round 141, no version bump). Byte-identical `ok=2 changed=1 skipped=1` cold, `changed=0` warm, both engines. |
| robertdebock.update_pip_packages | ✅ Clean (round 141, no version bump). Neither host has pip/pip3 installed - `block/rescue` path exercised identically, byte-identical `ok=5 changed=0 skipped=4 rescued=2` cold and warm. |
| robertdebock.container_docs | ✅ Clean (round 141, no version bump). `/etc/dpkg/dpkg.cfg.d/excludes` doesn't exist on Ubuntu 22.04 by default so the `regex_replace`-with-backreference task never actually exercises, but the `stat:`-gated skip is byte-identical `ok=2 changed=0 skipped=3` cold and warm, both engines. |
| robertdebock.molecule | ✅ Clean (round 141, no version bump). Fails identically on both engines - the role's own `molecule_requirements` default doesn't include `python3-pip` on Ubuntu, so `pip:` has no `pip3` binary to use. Byte-identical `ok=5 changed=0 failed=1`, an external role gap not a crystal-ansible bug. |
| robertdebock.php_fpm | ✅ Clean (round 141, no version bump). Real `.j2` template rendering (`php-fpm.conf`, `www.conf`) - byte-identical `ok=15 changed=4 skipped=1` cold, config files byte-for-byte identical between hosts, `php8.1-fpm` active on both, fully idempotent warm (`changed=0`). |
| robertdebock.metricbeat | ✅ Clean (round 141, no version bump). Fails identically - no Elastic apt repo configured (external dependency gap, not this role's job), byte-identical `ok=8 changed=0 failed=1 skipped=1`. |
| robertdebock.powertop | ✅ Clean (round 141, no version bump). Exercises `import_role: robertdebock.service` with `vars:` overrides - byte-identical `ok=9 changed=3 skipped=7` cold, fully idempotent warm (`changed=0`). |
| robertdebock.digitalocean_agent | ✅ Clean (round 141, no version bump). Real `apt_key`/repo-file/`do-agent` install - byte-identical `ok=6 changed=4` cold, `changed=0` warm; `skipped` count differs (1 vs 0) due to the already-documented `rpm_key` unimplemented-plugin gap (rounds 62/101/106/130), not a new issue. 10th consecutive clean/known-gap result this session - marathon paused per user's streak rule. Retested on RHEL in round 142 - `rpm_key` now real, byte-identical incl. `skipped` count. |
| robertdebock.epel | ✅ Fixed (round 142, 0.9.449-0.9.450). First RHEL-family (Rocky 9.6) round. Found: `ansible.builtin.rpm_key` entirely unimplemented (new `plugins/rpm_key.cr`); `package.cr`'s dnf backend falsely read a URL-based RPM (`epel-release-latest-9.noarch.rpm`) as "already installed" via a broken `rpm -q <url>` pre-check (real `rpm -q` queries a URL/path argument as a FILE, not an installed-name lookup). Byte-identical `ok=4 changed=2 skipped=1` cold, fully idempotent warm, after both fixes. |
| robertdebock.selinux | ✅ Fixed (round 142, 0.9.451-0.9.452). `ansible_selinux` was entirely unimplemented as a fact - `when: ansible_selinux.status is defined` always evaluated false, silently skipping SELinux management even on a real SELinux-capable host (new fact collection in `facts.cr`). `ansible.posix.seboolean` was also entirely unimplemented (new `plugins/seboolean.cr`). Verified for real: rebooted the target into SELinux-enforcing, then toggled `httpd_can_network_connect` and confirmed via `getsebool` on the host - genuinely applied, not just error-free. Fully idempotent warm rerun. |
| robertdebock.remi | ✅ Clean (round 142, no version bump - covered by round 142's rpm_key/package.cr URL-install fixes). Byte-identical `ok=5 changed=3` cold, `changed=0` warm. |
| robertdebock.rpmfusion | ✅ Fixed (round 142, 0.9.453-0.9.454). Found 2 more real bugs beyond epel's: (1) unquoted `curl` URL let embedded `&` characters shell-split the command into background jobs - a real key URL (`https://rpmfusion.org/keys?action=AttachFile&do=get&target=...`) silently fetched nothing, both `apt_key.cr` and `rpm_key.cr` fixed via the existing `shell_single_quote` helper; (2) `ansible.builtin.yum` entirely unimplemented (new `plugins/yum.cr`) and dnf/yum never supported the `update_cache: true`-with-no-`name:` idiom (ported package.cr's existing exception to dnf.cr/yum.cr). Fully idempotent warm rerun, byte-identical `ok=7 changed=0`. |
| robertdebock.powertools | ✅ Clean (round 142, no version bump). Fails identically both engines - Rocky ships no `powertools` repo file (only `crb`), a role/environment mismatch, not a crystal-ansible bug. Byte-identical `ok=2 changed=1 failed=1`. |
| robertdebock.zabbix_repository | ✅ Clean on RHEL (round 142, no version bump - covered by round 142's other fixes). Retested on Rocky 9.6 (previously only tested on apt in round 140); crypto-policy switch + rpm_key trust + dnf package install all matched task-by-task after accounting for self-inflicted host-state contamination from an earlier interrupted attempt (documented in `KNOWN_MISSING.md`). Fully idempotent warm rerun. |
| robertdebock.update_package_cache | ✅ Fixed on RHEL (round 142, 0.9.455). Previously only tested on apt (round 140). `package.cr`'s dnf/yum `update_cache`-only path always reported `changed: true`; real `ansible.builtin.package`'s dnf backend reports `changed: false` in practice (its internal libdnf5 helper never sets a `changed` key). Fixed to match apt/dnf's real differing semantics. Byte-identical `changed=0` cold and warm after the fix. |
| robertdebock.python_pip | ✅ Clean on RHEL (round 142, no version bump). Previously only tested on apt (round 58). Byte-identical `ok=3 changed=1 skipped=5` cold, fully idempotent warm. |
| robertdebock.cron | ✅ Clean on RHEL (round 142, no version bump). Previously only tested on apt (round 53). Cold run showed one non-reproduced divergence (a `lineinfile` task) explained as likely host-state drift from this round's unusually heavy reuse of the same host pair - see `KNOWN_MISSING.md`; a clean warm rerun matched byte-for-byte (`ok=9 changed=0 skipped=6`) on both engines. |
| robertdebock.ca | ⚠️ Blocked by an already-documented, pre-existing gap (round 138, no version bump for the role itself - see the separate `ansible.builtin.slurp` entry below for what WAS fixed this round). The role's own real-CA-cert-chain generation depends on `community.crypto.openssl_csr`/`openssl_privatekey`/`openssl_certificate`, none implemented - same class as the already-documented `community.crypto.x509_certificate` gap (round 90's prosody entry). A full, correct X.509 CA workflow (CSR generation, extensions, CA signing matching Python's `cryptography` library exactly) is a substantial undertaking, out of scope for a single round - documented, not attempted. |
| robertdebock.firewall | ✅ Clean (round 138, 0.9.446). 1 real bug found and fixed: `community.general.ufw`'s `state: enabled`/`disabled` used the `ufw enable`/`disable` command's own exit code as `changed` - real ufw exits 0 (printing "Firewall is active and enabled on system startup") whether or not anything actually changed, so it never converged on a warm rerun. Fixed by porting real ufw.py's own pre-state comparison (`ufw status verbose` before the operation, checking for " active") instead of the command's exit code - matches for both real and check-mode runs. Live-reverified: `ok=10 changed=2 skipped=8` cold, `ok=10 changed=0 skipped=8` idempotent warm rerun on both after the fix, `ufw status` active with SSH still allowed on both hosts throughout. |
| robertdebock.elastic_repo | ✅ Clean (round 138, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=6 changed=2 skipped=5` cold (skip-count off by one is the pre-existing, already-documented `ansible.builtin.rpm_key` unimplemented-module gap - RHEL/yum-only, zero functional impact on Ubuntu since both engines already skip that whole branch via `when: ansible_pkg_mgr == "yum"`), `ok=6 changed=0` idempotent warm rerun on both. |
| robertdebock.subversion | ✅ Clean (round 138, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=3 changed=1 skipped=1` cold, `ok=3 changed=0 skipped=1` idempotent warm rerun on both. |
| robertdebock.storage | ✅ Clean (round 138, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=4 changed=0 skipped=31` on both (default config has no real disks/volumes/filesystems/mounts to manage, so every include: gracefully skips identically on both engines). |
| *(not a Galaxy role - real module implementation)* `ansible.builtin.slurp` | ✅ Implemented (round 138, 0.9.445). Entirely unimplemented before - found via `robertdebock.ca`'s own "generate_ca_certs \| Save ca certificate" task (reading a just-generated cert file's content back to embed elsewhere). New `plugins/slurp.cr`: `src`/`path` alias, base64-encoded by default (`armor: true`), raw utf-8 when `armor: false`, real Ansible's own file-not-found/not-readable/is-a-directory error messages. Full integration spec added (5 cases). |
| robertdebock.apt_repository | ✅ Clean (round 137, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=4 changed=1 skipped=2` cold, `ok=4 changed=0 skipped=2` idempotent warm rerun on both, exercised the `ppa:` shorthand (`ppa:deadsnakes/ppa`), `/etc/apt/sources.list.d/ppa_deadsnakes_ppa_jammy.list` present identically on both. |
| robertdebock.common | ✅ Clean (round 137, 0.9.442, 0.9.444). 2 real bugs found and fixed: `common_hostname: "{{ inventory_hostname }}"` (a role default whose own value is itself a `{{ }}` expression, needing recursive re-templating) silently rendered to the literal STRING "localhost" on every host, regardless of the real per-host name - `VarSubstitutor`'s own default (`host_name: String = "localhost"`) clobbered `vars["inventory_hostname"]` (already correctly set to the real host) whenever an internal re-render call site omitted `host_name:`, which `CrinjaRenderer#prepare_crinja_vars`'s own inner substitutor did - the role's own "Set hostname" task silently renamed every managed host to "localhost". Also, `ansible.builtin.reboot` (notified by "Set hostname"/"Fill /etc/hosts", `common_reboot: true` by default) was entirely unimplemented - see its own dedicated entry below. Live-reverified after both fixes: hostname correctly set to the real per-host value, "Reboot" handler fires and the host genuinely reboots, matching real ansible-playbook. |
| robertdebock.java | ✅ Clean (round 137, no version bump - zero crystal-ansible bugs found). Needed `java_version: "17"` override (the role's own default, 19, has no apt candidate on Ubuntu 22.04 - external, reproduces identically on real ansible-playbook). Byte-identical `ok=9 changed=2 skipped=12` cold, `ok=9 changed=0 skipped=12` idempotent warm rerun on both, `openjdk 17.0.19` identical on both. |
| robertdebock.tomcat | ✅ Clean (round 137, 0.9.442 - same fix as `.common` above, found via this role instead). 1 real bug found and fixed: `import_role: name: robertdebock.service`'s own `vars:` rendering (`render_include_role_vars`) never preserved a scalar bool/int/float's real JSON type when the raw value was itself a bare `{{ }}` expression - `enabled: "{{ instance.service_enabled | default(tomcat_service_enabled) }}"` (a real boolean default) landed as the Python-repr STRING `"True"` instead of a real JSON boolean, failing the included role's own `item.enabled is boolean` assert outright (same bug class as the well-documented Python-repr-list one, just for a scalar bool). Fixed via a shared `evaluate_bare_mustache_preserving_type` helper (used by both `render_include_role_vars` and `render_task_vars`, which had the identical gap) that evaluates a bare whole-value `{{ }}` span through Crinja's own `evaluate_value!` instead of round-tripping through a String. Needed `robertdebock.service` installed separately (undeclared `import_role:` dependency, same recurring gotcha as several earlier rounds). Live-reverified: `ok=60 changed=2 skipped=14` cold (crystal_host had leftover partial-run state from the pre-fix attempt, so its own changed= count isn't directly comparable to a from-scratch host), fully idempotent warm rerun on both (`changed=0`), `tomcat` active and serving HTTP 200 on both. One pre-existing, already-documented cosmetic gap re-confirmed: `unarchive: creates:` short-circuits as `ok` here instead of real Ansible's `skipping` categorization. |
| robertdebock.zabbix_proxy | ✅ Clean (round 137, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=14 changed=4` cold, `ok=13 changed=1` warm on both - `zabbix-proxy` fails to actually start identically on both (needs a real DB backend this round didn't provision), an external gap not an engine issue. |
| robertdebock.update | ✅ Clean (round 137, 0.9.443). 1 real bug found and fixed: `apt: {update_cache: true, upgrade: dist, ...}` let the cache-refresh step's own `changed = true` unconditionally leak into the task's overall changed status, even when the subsequent dist-upgrade genuinely found "0 upgraded, 0 newly installed, 0 to remove" - real Ansible's own apt.py only lets a cache refresh alone decide `changed:` when no `name:`/`upgrade:`/`deb:` is also given. Fixed to match that exact condition. Live-reverified: `ok=7 changed=0 skipped=5` matching real ansible-playbook's own idempotent recap once the system was actually up to date (task-name-prefix and unimplemented-zypper/pacman/apk-module skip-line differences are cosmetic/pre-existing, not new). |
| *(not a Galaxy role - real module implementation)* `ansible.builtin.reboot` | ✅ Implemented (round 137, 0.9.444). Entirely unimplemented before (silently dropped at parse time) - found via `robertdebock.common`'s and `.update`'s own reboot-on-change handlers, both common real-world idioms. Architecturally can't be a normal remotely-uploaded plugin binary (the process would die with the machine mid-reboot) - implemented as a controller-side special case (`TaskExecutor#execute_reboot`): issues the reboot command tolerating the connection dying mid-command, then polls a trivial remote command until the host comes back or `reboot_timeout` is exceeded. Excluded from SSH batching and the eager per-play plugin pre-upload pass (same treatment `delegate_to:`/`connection:` already get, for the same "no single shared connection survives this" reason). Live-verified end-to-end: a real reboot task against a live Atlantic.net host completed, the host genuinely rebooted (`uptime` showed 0 min after), and the playbook correctly waited, reconnected, and continued. `local` connection is intentionally refused (would reboot the controller's own machine). |
| robertdebock.npm | ✅ Clean (round 136, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=4 changed=2 skipped=0` cold, `ok=4 changed=0` idempotent warm rerun on both. |
| robertdebock.ruby | ✅ Clean (round 136, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=2 changed=1` cold, `ok=2 changed=0` idempotent warm rerun on both. |
| robertdebock.ca_certificates | ✅ Clean (round 136, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=2 changed=0` cold and warm on both. |
| robertdebock.backup | ✅ Clean (round 136, 0.9.440-0.9.441). 3 real bugs found and fixed: `community.general.archive` crashed the WHOLE archive build on a symlink-to-directory member (Debian's own default `/var/spool/mail -> ../mail`, this role's own default backup target) - fixed with a real tar SYMLINK entry (full parity) and a documented zip-format-capability skip (Crystal's stdlib Zip writer has no Unix-attrs API); task-level `connection:` (distinct from `delegate_to:` - changes HOW a task's module runs, not WHICH host's vars apply) was never parsed at all, so the role's own "Create backup_directory" (`connection: local`, writes to the controller's filesystem) silently ran against the real remote target instead - fixed by parsing it and threading it into `ansible_connection` for that task's own dispatch, plus excluding it from SSH batching (same treatment `delegate_to:` already gets). Needed `ansible_python_interpreter=/usr/bin/python3` in the inventory (real ansible-playbook's own `connection: local` sub-task otherwise inherits the REMOTE host's auto-discovered python3.10 path, which this sandbox's controller doesn't have - an environment mismatch, not a role or engine bug). Live-reverified: byte-identical `ok=12 changed=2 skipped=5` cold, `ok=12 changed=0 skipped=5` idempotent warm rerun on both, `backups/<hostname>/` correctly created on the controller by both engines. |
| robertdebock.restore | ✅ Clean (round 136, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=8 changed=1 skipped=10` cold, `ok=8 changed=0 skipped=10` idempotent warm rerun on both. |
| robertdebock.turn | ✅ Clean (round 136, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=22 changed=3 skipped=1` cold, `ok=21 changed=0 skipped=1` idempotent warm rerun on both, `coturn` active on both, rendered `turnserver.conf` identical apart from each host's own real IP/hostname. |
| robertdebock.dns | ✅ Clean (round 135, 0.9.433-0.9.435). 3 real bugs found and fixed, none role-specific - all three were general engine gaps this role happened to be the first to exercise: `--limit` was parsed but never actually applied anywhere, silently running every play against the WHOLE inventory regardless (fixed in `crystal-play.cr`, plus a regression spec - previously zero coverage for the flag); the round 132 "skip bash -c" local-exec optimization's shell-metacharacter detection missed a leading `NAME=value` env-assignment prefix (`DEBIAN_FRONTEND=noninteractive apt-get install ...`, apt.cr/package.cr's own real command), breaking every local-connection apt install; and a role-relative `copy: src:` (nested inside a `block:`, over a real SSH connection - previously untested combination) never got staged to the remote host at all when the resolved path was relative (the common case), because the "does this need staging" gate required an absolute path. Live-reverified: `ok=24 changed=9 skipped=7` cold and `ok=22 changed=0 skipped=7` idempotent warm rerun, byte-identical to real ansible-playbook on both, `named.conf.local` md5-identical, bind9 active on both. |
| robertdebock.certbot | ✅ Clean (round 135, no version bump - zero crystal-ansible bugs found). Needed `certbot_system: standalone`/`certbot_domains: [example.test]`/`certbot_ci_mode: true` overrides (the role's own default `certbot_system: apache` + a bare Atlantic.net hostname with no FQDN fails its own `"." in item` assert identically on both engines - external, not tested further). Byte-identical `ok=7 changed=2 skipped=3` cold, `ok=7 changed=0 skipped=2` idempotent warm rerun on both. |
| robertdebock.openssl | ✅ Clean (round 135, 0.9.436). 1 real bug found and fixed: a when:-gated `include_tasks:` (the role's own looped `include_tasks: create.yml`, `loop: "{{ openssl_items }}"` undefined by default, gated by `when: openssl_items is defined`) incremented recap `ok` unconditionally BEFORE its own when: check ran, then `skipped` again once that check failed - double-counted as both for the same single skipped task, inflating `ok=` by one. Fixed in `execute_include_tasks`/`run_include_tasks_once` (the multi-host batched path already partitioned run/skip hosts correctly and didn't have this bug). Byte-identical `ok=10 changed=1 skipped=2` cold, idempotent warm rerun on both after the fix. |
| robertdebock.facts | ✅ Clean (round 135, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=5 changed=2 skipped=1` cold, `ok=4 changed=0 skipped=1` idempotent warm rerun on both, `/etc/ansible/facts.d/custom.fact` md5-identical. |
| robertdebock.swap | ✅ Clean (round 135, 0.9.438). 2 real bugs found and fixed: `community.general.filesystem` was entirely unimplemented (new `plugins/filesystem.cr` - state present/absent, blkid-based existing-fs detection, force/opts, most Linux fstypes; `resizefs:`/`uuid:` not implemented) - the role's own "Make swap file system" (`fstype: swap` on a freshly dd'd swap file) silently dropped; and a `block:`'s own `notify:` (the role's own "Manage swap files." block, wrapping 4 nested tasks none of which have their own notify:, itself notifying "Run swapon") was silently dropped at TWO levels - `parse_block_task` never parsed the `notify:` key at all, and `execute_block`/`execute_block_multi` never checked it even when present, only ever forwarding an individual nested task's own notify:. Both fixed. Live-reverified: byte-identical `ok=11 changed=5 skipped=2` cold, `ok=10 changed=0 skipped=2` idempotent warm rerun on both - the role's own handler (`swapon` with no arguments, no `-a` flag) doesn't actually activate the swap file on EITHER engine (confirmed identically broken on real ansible-playbook too - `swapon --show` empty on both hosts after a full run), a real external role bug, not an engine issue. |
| robertdebock.sudo_pair | ✅ Clean (round 135, 0.9.439). 1 real bug found and fixed: `zip_changed?`'s idempotency check compared a symlink zip member's DEREFERENCED content (`md5sum < dest_path`, shell redirection always follows symlinks) against the archive's own raw member content (a zip symlink entry's content IS the short target-path string, not the target's content) - permanently mismatched even though extraction was byte-correct, so the role's own "Download and unpack sudo_pair" (square/sudo_pair's GitHub release zip symlinks a shared LICENSE/README into several sub-crate dirs) was re-extracted on every single rerun. Fixed by comparing `readlink`'s own output instead of dereferencing. Needed `unzip`, a full `rustup`-installed toolchain (`cargo`/`rustc` symlinked into `/usr/local/bin`, the role's own PATH override), and `gcc` installed separately - the role's own `sudo_pair_requirements` only lists `libclang-dev`, missing its own real build dependencies (external role gap, reproduces identically on real ansible-playbook too). Live-reverified: byte-identical `ok=8 changed=4 skipped=4` cold, `ok=8 changed=0 skipped=4` idempotent warm rerun on both after the fix. |
| robertdebock.mitogen | ✅ Clean (round 133, 0.9.427). 1 real bug found and fixed: `ansible_facts['python']` was a flat string (executable path) stored under two invented keys (`ansible_python`/`ansible_python_version`) that don't exist under those names in real Ansible - real Ansible's own `PythonFactCollector` exposes a single nested dict (`version.major`/`.minor`/`.micro`, `version_info`, `executable`, `has_sslcontext`, `type`). The role's own `python{{ ansible_facts['python'].version.major }}` command construction resolved to the literal string `pythonundefined` instead of `python3`, crashing the very first real task. Rewrote `facts.cr`'s `gather_python_facts` to introspect the interpreter itself (`python3 -c '...json.dumps(...)'`) and build the correct nested structure, matching real Ansible's schema exactly. Live-reverified on a fresh Atlantic.net pair: `ok=7 changed=3 failed=0` identical cold, fully idempotent warm rerun on both (recap `skipped` vs `ok` categorization for the `unarchive: creates:` short-circuit differs cosmetically, a separate pre-existing gap, not new). |
| robertdebock.revealmd (+ robertdebock.npm, robertdebock.service) | ⚠️ Engine clean (round 132, no version bump) but not fully runnable on this cloud image regardless of engine - byte-identical `ok=17 changed=6 failed=0 skipped=8` on both, identical rendered `/etc/systemd/system/revealmd.service`. `reveal-md`'s systemd service fails to start identically on both engines (`journalctl`: `Error [ERR_UNKNOWN_BUILTIN_MODULE]: No such built-in module: node:fs/promises`) - Ubuntu 22.04's default apt `nodejs` package (v12.22.9) is too old for the latest npm `reveal-md` release, which needs Node >= 14. Not an engine bug - an environment/Node-version prerequisite gap. |
| robertdebock.vagrant | ✅ Clean (round 131, 0.9.426). 1 real bug found and fixed: `pip.cr`'s `name:` never handled a `{{ }}`-templated list variable rendering to its own bracketed text form (same "Python-repr-list JSON" class as apt.cr/package.cr/dnf.cr/find.cr) - the role's own `name: "{{ vagrant_pip_packages }}"` (an empty list on Debian) fed pip the literal string `"[]"`, crashing with `ERROR: Invalid requirement: '[]'` instead of real Ansible's clean `if name:` falsy-empty-list no-op. Fixed with a `normalize_name` helper (empty list -> nil/no-op, single-element list -> unwrap, matching real `pip.py`'s `required_one_of`/truthiness semantics exactly); multi-package `name:` lists remain a separate, pre-existing, documented single-package-only scope limit. Live-reverified: `ok=6 changed=0 failed=0` identical on both engines after the fix (skip-count differs only via the pre-existing `community.general.zypper` unimplemented-module gap, same class as `rpm_key`/`zypper_repository`/`seport`). |
| robertdebock.virtualbox | ✅ Clean (round 130, no version bump - zero crystal-ansible bugs found). Byte-identical package/repo state on both engines: `virtualbox-6.1` installed identically (`dpkg -l` match), `/etc/apt/sources.list.d/download_virtualbox_org_virtualbox_debian.list` byte-identical, cold `changed=4` and warm `changed=0` (fully idempotent) on crystal-ansible. Skip-count differs from real Ansible (1 vs 3) - the pre-existing, already-documented `ansible.builtin.rpm_key`/`community.general.zypper_repository` unimplemented-module scope gap (same class as `seport`/`seboolean`/`apache2_module`), not a new bug. |
| robertdebock.upgrade | ✅ Clean (round 129, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=0 failed=0 skipped=1`, warm rerun identical (fully idempotent) - the role's own "Upgrade package" task has no `register:` on the preceding "Check if package is installed" task, so its `when: ...results is defined` guard always skips (undefined-var short-circuit), reproduced identically on both engines with `upgrade_packages: [vim, curl]` set via extra vars. |
| robertdebock.tigervnc | ⚠️ Engine clean (round 128, no version bump) but not fully runnable on this cloud image regardless of engine - `journalctl`/`ok=13 changed=6 failed=1 skipped=2` byte-identical on both engines, both failing the exact same "Restart tigervnc" handler with an identical error. Root cause confirmed via `journalctl -u vncserver@:1.service`: the role's own `xstartup` execs `gnome-session` (the role's default `tigervnc_desktop_session`), which isn't installed on a minimal cloud image - the X session "cleanly exits too early", byte-identical on both. Rendered `xstartup`/`vncserver@.service` templates confirmed byte-identical between engines. Not an engine bug - an environment prerequisite (no desktop environment installed) the role doesn't itself install. |
| robertdebock.sosreport | ⚠️ Engine clean (round 127, no version bump) but not runnable on this cloud image regardless of engine - the role's `sosreport_packages` default (`sos`) has no installation candidate at all in Ubuntu 22.04's apt sources (confirmed via `apt-cache policy sos` even with `universe` enabled - the package genuinely doesn't exist for this release). Both engines fail identically at the "Install sosreport" task (`ok=3 changed=0 failed=1` both), same error message content. A role/distro package-availability gap, not a crystal-ansible issue. |
| robertdebock.lynis | ✅ Clean (round 126, 0.9.425). 1 real bug found and fixed: `cron.cr`'s `execute_file` used a relative `cron_file:` value (e.g. `cron_file: lynis`) literally as the write path, resolving to `./lynis` relative to the plugin's own cwd - real Ansible's `cron.py` resolves any non-absolute `cron_file:` against `/etc/cron.d` (`os.path.join('/etc/cron.d', cron_file)`). The role's own "Schedule a run of lynis" task (`cron_file: lynis`) reported `changed: true` but silently never wrote `/etc/cron.d/lynis` at all. Fixed to match real Ansible's resolution exactly. Live-reverified: `/etc/cron.d/lynis` byte-identical between engines after the fix, `ok=10 changed=0 failed=0` warm rerun identical on both. |
| robertdebock.terraform | ✅ Clean (round 125, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=1 failed=0`, warm rerun `ok=4 changed=0 failed=0` (fully idempotent) - single-task `unarchive: remote_src: true` install from a HashiCorp release URL. `/usr/local/bin/terraform` byte-identical (`md5sum` match) between engines, `terraform version` `1.0.11` on both, verified live. |
| robertdebock.xrdp | ✅ Clean (round 124, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=9 changed=5 failed=0`, warm rerun `ok=8 changed=0 failed=0` (fully idempotent) - exercises `ini_file` with a real loop, `copy`, and `until:`/`retries:`. `xrdp.ini`'s `crypt_level` byte-identical and `xrdp` service active on both, verified live. |
| robertdebock.auto_update | ✅ Clean (round 123, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=9 changed=2 failed=0`, warm rerun `ok=9 changed=0 failed=0` (fully idempotent). Rendered `/etc/apt/apt.conf.d/10periodic` (`comment` filter's `//`-style rendering) byte-identical on both, verified live. |
| robertdebock.modprobe | ✅ Clean (round 122, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=5 changed=1 failed=0`, warm rerun `ok=5 changed=0 failed=0` (fully idempotent). `br_netfilter`/`overlay` kernel modules loaded identically (`lsmod` byte-identical) on both, verified live. |
| robertdebock.gotop | ✅ Clean (round 121, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=1 failed=0`, warm rerun `ok=4 changed=0 failed=0` (fully idempotent). `gotop` binary byte-identical (same size, version, mtime) on both, verified live. |
| robertdebock.code | ✅ Clean (round 120, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=3 failed=0 skipped=1`, warm rerun `ok=4 changed=0 failed=0 skipped=1` (fully idempotent) - exercises `apt_key`, `apt_repository`, and a real ~100MB package install together. `code` package version identical on both, verified live. |
| robertdebock.environment | ✅ Clean (round 119, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=2 changed=1 failed=0`, warm rerun `ok=2 changed=0 failed=0` (fully idempotent). `/etc/profile.d/custom.sh`'s `dict2items`-driven lineinfile output byte-identical on both, verified live. |
| robertdebock.scripts | ✅ Clean (round 118, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=9 changed=2 failed=0`, warm rerun `ok=9 changed=0 failed=0` (fully idempotent). Rendered `.j2` script (block-tag loop, `comment` filter, conditional block) byte-identical on both, verified live. |
| robertdebock.maintenance | ✅ Clean (round 117, 0.9.424). 1 real bug found and fixed: `apt.cr`'s `autoclean:`/`autoremove:`/`clean:` changed-detection used an empty-vs-non-empty-stdout heuristic, but `apt-get autoclean` always prints boilerplate stdout regardless of whether anything was removed - `autoclean: true` on an already-clean cache always reported `changed: true`. Fixed to match real Ansible's own per-operation marker-string check (`apt.py`'s `CLEAN_OP_CHANGED_STR`). Live-reverified: byte-identical `changed=0` on a clean cache and `changed=1` identically on both once real cache garbage existed. |
| robertdebock.functions | ✅ Clean (round 116, 0.9.423). 2 real bugs found and fixed: `format_value`'s String case unconditionally stripped every rendered value's leading/trailing whitespace (a real Jinja2 never does this - affected every string-valued variable, not just this role's test data), and the vendored Crinja fork's `wordwrap` filter did fixed-width character chunking instead of real Python `textwrap.wrap`'s whole-word packing (fixed upstream in the fork, `crystal-play-0.9.12`). Also found `build.sh`'s staleness check never looked at `lib/` mtimes, silently no-opping a `shards update`-driven rebuild - fixed. Live-reverified: byte-identical `ok=23 changed=0 failed=0` cold and warm, all 27 `debug:` msg lines byte-identical to real Ansible including exact wordwrap line breaks. |
| robertdebock.umask | ✅ Clean (round 115, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=1 failed=0`, warm rerun `ok=4 changed=0 failed=0` (fully idempotent). `~/.bashrc`'s appended `umask 0022` line byte-identical on both, verified live. |
| robertdebock.types | ✅ Clean (round 114, no version bump - zero crystal-ansible bugs found). Byte-identical `ok=17 changed=0 failed=0` recap on both engines, cold and warm - every `is string`/`is number`/`is boolean`/`is float`/`is mapping`/`is iterable` test and `type_debug` call agreed with real Ansible. One cosmetic-only note: `debug: msg:` of a dict/list renders via Python repr (single quotes) on real Ansible vs compact JSON here - no assertion/result was affected. |
| robertdebock.test_connection | ✅ Clean (round 113, 0.9.422). 2 real bugs found and fixed: `ansible.builtin.ping` and `ansible.builtin.wait_for_connection` were both entirely unimplemented (new `plugins/ping.cr` and `plugins/wait_for_connection.cr`). Live-reverified: byte-identical `ok=10 changed=0 failed=0` recap and the role's own written result files byte-identical between engines. |
| robertdebock.tlp | ✅ Clean (round 112, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=2 failed=0 skipped=5`, warm rerun `ok=4 changed=0 failed=0 skipped=5` (fully idempotent). `tlp` service active and enabled on both, verified live. |
| robertdebock.unowned_files | ✅ Clean (round 111, 0.9.421; regression-reverified round 134, no version bump). 2 real bugs found and fixed: `find.cr`'s `paths:`/`patterns:`/`excludes:` never handled a `{{ }}`-templated list variable rendering to its own bracketed text form (same "Python-repr-list JSON" class as apt.cr/package.cr/dnf.cr), and `BasePlugin#native_stat`'s `pw_name`/`gr_name` fell back to the stringified uid/gid instead of an empty string for an orphaned owner, breaking the role's own `item.pw_name | length == 0` unowned-file detection entirely. Live-reverified: byte-identical `ok=13 changed=3 failed=0 skipped=1` recap and per-item loop results (including the orphaned file's empty `pw_name`/`gr_name`) on both engines. Round 134: re-ran cold+warm on a fresh pair, still byte-identical, no regression. |
| robertdebock.ulimit | ✅ Clean (round 110, 0.9.420). 2 real bugs found and fixed in `pam_limits.cr`: a `comment:` was written as a separate preceding line instead of a trailing inline `\t#comment` on the entry's own line, and a brand new entry was inserted before a `# End of file` marker instead of always appended at the true end (neither matches real `community.general.pam_limits`'s own module source). Also fixed: updating an existing entry's value now preserves its own trailing comment when no new one is given. Live-reverified: byte-identical `ok=8 changed=1 failed=0` cold and `ok=8 changed=0 failed=0` warm, actual written `limits.conf` entries byte-identical between engines. |
| robertdebock.kubectl | ✅ Clean (round 109, 0.9.419). 1 real bug found and fixed: `lookup('url', ...)` always returned a JSON-array string regardless of `wantlist=True`, so a call with no `wantlist=True` (the role's own `kubectl_url:` version-file fetch) spliced the literal text `["v1.31.0"]` into the download URL instead of the plain string `v1.31.0`, a 404. Fixed to comma-join into a plain string when `wantlist=True` is absent, matching real Ansible's `lookup()` semantics exactly. Live-reverified: byte-identical `ok=2 changed=1 failed=0` cold, `ok=2 changed=0 failed=0` warm, `kubectl version --client` identical (`v1.31.0`) on both. |
| robertdebock.packer | ✅ Clean (round 108, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=3 changed=1 failed=0`, warm rerun `ok=3 changed=0 failed=0` (fully idempotent). `packer version` identical (`1.7.8`) on both, verified live. |
| robertdebock.hashicorp | ✅ Clean (round 107, 0.9.417-0.9.418). 2 real bugs found and fixed: `assert.cr`'s standalone plugin binary never required `jinja_filters.cr`, so an `is regex(...)`/`is version(...)`/etc test inside `that:` silently failed even when the identical condition worked via `when:` (Crinja's custom test library wasn't linked into that specific binary); and `apt_repository.cr`'s cache-update validation trusted `apt-get update`'s own exit code (stays 0 for a GPG signature failure), missing the failure real Ansible's python-apt-based check catches, which let a broken signed-by= line survive instead of being rolled back and then get duplicated by a rescue: retry. Live-reverified: byte-identical `ok=14 changed=4 failed=0 skipped=5 rescued=1` on both (the `rescued=1` itself is real - `apt.releases.hashicorp.com`'s signed-by= setup genuinely fails GPG verification on both engines), single clean sources.list.d entry, `terraform v1.15.8` identical on both. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical including the same real `rescued=1` GPG-verification hit, no regression. |
| robertdebock.openbao | ✅ Clean (round 106, no version bump - documented not fixed). `ansible.builtin.rpm_key` remains unimplemented (same cosmetic-scope gap class as `seboolean`/`seport`/`apache2_module`) - zero runtime impact on Ubuntu, only the `skipped` count differs (2 real vs 1 crystal). `ok=12 changed=5 failed=0` identical cold, `ok=12 changed=0 failed=0` identical warm. `openbao 2.5.0` byte-identical on both, verified live. |
| robertdebock.docker_compose | ✅ Clean (round 105, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=4 changed=1 failed=0 skipped=2`, warm rerun `ok=4 changed=0 failed=0 skipped=2` (fully idempotent). `docker-compose --version` identical (`1.29.2`) on both, verified live. |
| robertdebock.docker | ✅ Clean (round 104, 0.9.416). 1 real bug found and fixed: `CrinjaRenderer#prepare_crinja_vars` re-rendered a nested-template variable (`docker_pip_packages: "{{ ... }}"`, resolving to a real empty list) but never re-parsed the result back to JSON, so `| length` measured the stringified text's character count instead of the list's element count, breaking the role's own `when: docker_pip_packages | length > 0` guard and feeding `pip: name: "[]"` a literal invalid package name. Live-reverified: warm-rerun recap `ok=4 changed=0 failed=0 skipped=2` identical on both, `docker` service active and `docker run hello-world` working on both. |
| robertdebock.alternatives | ✅ Clean (round 103, 0.9.415). 1 real bug found and fixed: `community.general.alternatives` entirely unimplemented (new `plugins/alternatives.cr`), plus a self-inflicted regex idempotency bug found and fixed before shipping (Crystal's `/m` flag = multiline anchors + dot-matches-newline combined, unlike Python's `re.MULTILINE`). Live-reverified: byte-identical `update-alternatives --display` output, cold `ok=6 changed=2 failed=0` and warm `ok=6 changed=0 failed=0` match real Ansible exactly. `family:` (RHEL-only) not implemented - no EL/Fedora host available. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical `update-alternatives --display` and fully idempotent, no regression. |
| robertdebock.aide | ✅ Clean (round 102, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=5 changed=3 failed=0`, warm rerun `ok=4 changed=0 failed=0` (fully idempotent). `aide.db` initialized and `@daily aide --check` cron job identical on both, verified live. |
| robertdebock.clamav | ⚠️ Role's own bug confirmed identical on both engines (round 101, no version bump) - `freshclam_private_mirrors` used without a default, both engines fail the "Download signatures" task (`ok=9 changed=3 failed=1` both). Also re-confirms `ansible.posix.seboolean` unimplemented (documented gap, not testable without an SELinux host). |
| robertdebock.cargo | ✅ Clean (round 100, no version bump - zero crystal-ansible bugs found). Byte-identical on both engines: cold pass `ok=6 changed=2 failed=0`, warm rerun `ok=6 changed=0 failed=0` (fully idempotent). `cargo 1.97.1`/`rustc 1.97.1` installed via rustup identically on both, verified live. |
| robertdebock.irslackd | ✅ Clean (round 99, 0.9.414). 1 real bug found and fixed in `npm.cr` (this codebase's own round-75 plugin): the `state: present` "already installed" check was gated on a truthy `name_version`, which is nil whenever `name:` is omitted (the role's own `path:`-only "install everything from package.json" task) - always reported `changed: true` unconditionally, never converging. Fixed by dropping that gate, matching real `community.general.npm`'s own `if missing:` check exactly. Live-reverified: `ok=22 changed=0 failed=0 skipped=8` on warm rerun matches real Ansible, `irslackd` service `active` on both. Also needed `npm`/`nodejs` and `robertdebock.service` installed separately. |
| robertdebock.earlyoom | ✅ Clean (round 98, 0.9.412-0.9.413). 2 real bugs. `git.cr`'s `resolve_ref` used `rev-parse <ref>` which returns an ANNOTATED tag's own object SHA (not the commit it points to) for `version: v1.6` - never matched `current_commit`'s always-a-real-commit-SHA, so every rerun re-checked-out and reported changed:true, never converging; fixed by peeling through `^{commit}`, matching real Ansible's own approach. Also, `community.general.make` was entirely unimplemented - new `plugins/make.cr` added, ported exactly from real Ansible's own module source (idempotency via `make ... -q`). Found via "Make earlyoom"/"Install earlyoom" both notified by the same task - "Make earlyoom" was silently dropped at parse time, so the earlyoom binary never got built and the copy-based "Install earlyoom" handler failed. Live-reverified: `ok=14 changed=0 failed=0 skipped=8` on warm rerun now byte-identical to real Ansible, `earlyoom` service `active` on both. |
| robertdebock.haveged | ✅ Clean (round 97, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=8 changed=4 failed=0` cold, `ok=7 changed=0 failed=0` warm - fully idempotent), `haveged` service `active` on both. |
| robertdebock.tailscale | ✅ Clean (round 96, 0.9.410-0.9.411; 1 more real bug found and fixed round 134, 0.9.428). 3 real bugs found and fixed round 96: (1) a PARSE-TIME crash ("Can't convert Bool to a JSON object key") took down the entire playbook - the role's own vars/main.yml uses a dict keyed by a bare YAML boolean (a real supported Ansible/Jinja2 idiom), which RoleLoader.load_vars_file's JSON round-trip crashed on; fixed with a recursive key-stringifying converter. (2) a real Crystal 1.20.3 stdlib HTTP::Client bug silently truncated a chunked HTTPS response body for pkgs.tailscale.com (200 OK, no error, wrong byte count) - apt_key.cr's url: fetch now shells out to curl instead. (3) apt_key.cr's keyring: parameter was entirely unimplemented, silently breaking any role pairing apt_key: keyring: with a matching apt_repository:/deb822 signed-by= path - fixed by threading --keyring through every apt-key subcommand. Round 134: the upstream role has since gained a Debian-13/Ubuntu-24.04 `deb822` repo path as a sibling `block:` to the older `sources.list` path, both `register:`-ing the same name (`_deb_repository`) - on Ubuntu 22.04 the `deb822` block is the one skipped, but its nested task's `register:` was never reapplied (a task skipped only via its ENCLOSING block's `when:`, not its own, never went through `register_skip_result`), so `_deb_repository` kept the sources.list block's real `changed: true`, wrongly running the downstream "Update apt cache" task instead of skipping it like real Ansible. Fixed. Live-reverified: `tailscaled` active and functional, recap byte-identical to real Ansible (`ok=7 changed=3` cold, `changed=0` warm) on both engines; skip-count diff is the pre-existing `rpm_key`/`zypper_repository`/`zypper` unimplemented-module gap (RHEL/SUSE-only), not new. |
| robertdebock.openvpn | ✅ Clean (round 95, 0.9.409). 1 real bug found and fixed (2 independent copies): a non-looped `include_tasks:` never credited itself as an `ok` in the recap tally, unlike real Ansible (which counts the include dispatch itself, separate from its expanded tasks). Fixed in both `execute_include_tasks`'s non-looped branch and the separate `execute_include_tasks_multi` batched path. Cosmetic only - the actual work (easy-rsa PKI, all copied files, service) was already byte-identical on both engines. Live-reverified: `ok=16` matches real Ansible exactly, cold and warm. |
| robertdebock.rundeck | ✅ Clean (round 94, no version bump). The role never installs a JRE at all - "Unpack rundeck" fails identically on both engines on a fresh host (external gap, not crystal-ansible). After manually installing `default-jre-headless`: identical on both engines (`ok=31 changed=10 failed=0 skipped=9` cold, `ok=29 changed=0 failed=0 skipped=9` warm - fully idempotent), `rundeck` service `active` on both. Also needed `robertdebock.service` installed separately (recurring undeclared dependency pattern). |
| robertdebock.roundcubemail | ⚠️ Real gap found, documented not fixed (round 93, no version bump). The role's own `notify: restart httpd` has no matching handler for the Debian family (a role bug) - real Ansible correctly refuses to even run at all (`ERROR! The requested handler ... was not found`), while crystal-ansible has no equivalent upfront notify-target validation and silently no-ops the unknown notify, completing the whole role "successfully" but leaving apache's new roundcube vhost never activated. Same broader "unresolvable reference silently dropped" design as unimplemented modules (rounds 75/90), now confirmed to also extend to notify: handler names - not changed this round, a much larger design decision than a targeted fix. |
| robertdebock.etherpad | ⚠️ Externally broken, not a crystal-ansible divergence (round 92, no version bump). The pinned Galaxy release is stale relative to upstream's current archive naming (`etherpad-lite-X` hardcoded, actual archive extracts to `etherpad-X`). Both engines ultimately fail to produce a working service, via different failure points: real Ansible's `unarchive` fails outright at extraction; crystal-ansible's `unarchive` succeeds (correctly-named directory) but the later systemd start step then fails on the role's own stale hardcoded path. Also needed `unzip` installed (real Ansible's `unarchive` shells out to it for .zip, crystal-ansible's own implementation doesn't need it) and `robertdebock.service` installed separately. |
| robertdebock.cntlm | ✅ Externally blocked identically on both engines (round 91, no version bump). The role's own default download mirror (`netcologne.dl.sourceforge.net`) doesn't resolve at all - byte-identical recap `ok=16 changed=0 failed=1 skipped=4` on both, real `ansible-playbook` fails at the exact same step. Also needed `robertdebock.service` installed separately (undeclared `import_role:` dependency, same gotcha as round 75's `robertdebock.node_red`). |
| robertdebock.prosody | ✅ Clean (round 90, no version bump). Needs `community.crypto` collection installed for real Ansible to even parse the playbook (statically `import_tasks:`'d `ssl.yml` references `community.crypto.x509_certificate` unconditionally). That module remains unimplemented in crystal-ansible (not new - same established "unresolvable module dropped, rest of playbook runs" design). Once installed: `prosody` service `active` on both, config content identical (only expected hostname differences), fully idempotent warm rerun on both. |
| robertdebock.systemd | ✅ Clean (round 89, 0.9.408). 1 real bug found and fixed: `ini_file.cr` never implemented real Ansible's `modify_inactive_option: true` default - a commented-out `#option=value` line should count as a match and get uncommented/replaced in place; crystal-ansible only ever matched active lines, so it appended a duplicate active line at the end of the section instead, leaving the stale commented line behind. Found via `systemd_journald: [{option: LineMax, value: 48k}]` against journald.conf's own upstream-commented defaults. Live-reverified: uncomments in place identically to real Ansible, no duplicate line. |
| robertdebock.tune2fs | ✅ Clean (round 88, no version bump - zero crystal-ansible bugs found; exercised with a real `maximum_count_mount: -1` setting, default is a no-op empty list). Identical on both engines (`ok=5 changed=0 failed=0 skipped=1` cold and warm), `tune2fs -l`'s "Maximum mount count" identical on both. |
| robertdebock.logwatch | ✅ Clean (round 87, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=8 changed=2 failed=0 skipped=1` cold, `ok=8 changed=0 failed=0 skipped=1` warm - fully idempotent). |
| robertdebock.sysstat | ✅ Clean (round 86, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=4 changed=3 failed=0` cold, `ok=4 changed=0 failed=0` warm - fully idempotent), `sysstat` service `active` on both, config byte-identical. |
| robertdebock.collectd | ✅ Functionally clean (round 85, no version bump). Identical on both engines (`ok=33 changed=4 failed=0 skipped=2` cold, `ok=32 changed=0 failed=0 skipped=2` warm - fully idempotent), `collectd` service `active` on both. Found (but did NOT fix - see `KNOWN_MISSING.md`) a real Crinja whitespace-control gap: explicit `-` markers (`-%}`/`{%-`) don't strip a full multi-line whitespace run, only the first line - cosmetic here (indentation/blank-line differences only, config still parses and behaves identically), a same-session fix attempt regressed 21 Crinja specs and was reverted. |
| robertdebock.gitea | ✅ Clean (round 84, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=12 changed=6 failed=0 skipped=1` cold, `ok=12 changed=0 failed=0 skipped=1` warm - fully idempotent), `gitea` service `active` and serving HTTP 200 on `:3000` on both. |
| robertdebock.keepalived | ✅ Clean (round 83, 0.9.407). 1 real bug found and fixed: a handler previously notified by an earlier task still ran at the implicit end-of-play flush even after a LATER task failed and halted the host - `HandlerRunner#run` had no way to know about halted hosts. The role's own default `keepalived_vrrp_instances` is empty, so `keepalived` genuinely has "no configuration to run" and its "Start keepalived" task times out identically on real `ansible-playbook` (external role/config gap). Live-reverified: `failed=1` now, matching real Ansible exactly (previously crystal-ansible additionally ran the notified "Restart keepalived" handler and failed a second time). |
| robertdebock.nfsserver | ✅ Clean (round 82, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=8 changed=4 failed=0 skipped=3` cold, `ok=6 changed=0 failed=0 skipped=3` warm - fully idempotent), `nfs-server` service `active` on both, `/etc/exports` byte-identical. The first cold-pass attempt on node1 hit an unrelated `unattended-upgrades` dpkg-lock race (not a crystal-ansible bug, see round 78's memory), resolved by killing the lock holder and retrying. |
| robertdebock.dhcpd | ✅ Clean (round 81, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=17 changed=5 failed=0 skipped=1` cold, `ok=16 changed=1 failed=0 skipped=1` warm), `/etc/dhcp/dhcpd.conf` byte-identical. `isc-dhcp-server` fails identically on BOTH engines (default config has no subnet matching this VM's network interface) - reproduces on real `ansible-playbook` too, an external role/environment gap. |
| robertdebock.logrotate | ✅ Clean (round 80, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=15 changed=1 failed=0 skipped=26` cold, `ok=15 changed=0 failed=0 skipped=26` warm - fully idempotent), `/etc/logrotate.conf` byte-identical. |
| robertdebock.auditd | ✅ Clean (round 79, 0.9.406). 1 real bug found and fixed: a handler notifying ANOTHER handler (`notify:` inside `handlers/main.yml` itself) was entirely unhandled - the role's own "Run augenrules" handler notifies "Load rules", which real Ansible runs within the same `flush_handlers` pass; crystal-ansible silently dropped "Load rules" (one fewer changed handler, `augenrules --load` never actually re-applied the regenerated rules). Live-reverified: `ok=44 changed=5 failed=0 skipped=5` identical to real Ansible's cold-run recap. |
| robertdebock.unbound | ⚠️ Real gap found, not fixable here (round 78, 0.9.405). The pinned Galaxy release's `./configure --enable-systemd` genuinely fails on stock Ubuntu 22.04 (missing `pkg-config`, so `libsystemd` isn't detected despite `libsystemd-dev` being installed) - reproduces identically on real `ansible-playbook`, which halts the play right there. 1 real crystal-ansible bug found and fixed alongside it: a failed HANDLER never halted the rest of the play for that host (`execute_handler_internal` was the one execution path missing a `halt_if_failed` call) - crystal-ansible kept running every task after the failing handler instead of stopping like real Ansible does. Live-reverified: crystal-ansible now halts at the same point as real Ansible. |
| robertdebock.ntp | ✅ Clean (round 77, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=15 changed=2 failed=0 skipped=2` cold, `ok=14 changed=0 failed=0 skipped=2` warm - fully idempotent), `ntp` service `active` on both, `/etc/ntp.conf` byte-identical. |
| robertdebock.fail2ban | ✅ Clean (round 76, no version bump - zero crystal-ansible bugs found; distinct from the already-tested `Oefenweb.fail2ban`). Identical on both engines (`ok=17 changed=5 failed=0 skipped=5` cold, `ok=16 changed=0 failed=0 skipped=5` warm - fully idempotent), `fail2ban` service `active` with `sshd` jail on both, `jail.local` byte-identical apart from the expected hostname-derived `sender` field. |
| robertdebock.node_red | ✅ Clean (round 75, 0.9.403-0.9.404). New `community.general.npm` plugin (entirely unimplemented before); `import_role:` entirely unhandled by the parser (silently dropped the whole task); `include_role:`/`import_role:` `vars:` renderer didn't recurse into Array/Hash-shaped values (a list-of-dicts `service_list:` landed with every `{{ }}` field unrendered). Live-reverified: `ok=14 changed=1 failed=0 skipped=8` identical both engines cold and warm. `node-red`'s own systemd service fails identically on BOTH engines - stock Ubuntu 22.04 `nodejs` (v12) is too old for current node-red (needs v22.9+), a real external role/environment gap confirmed on real ansible-playbook too. |
| robertdebock.tftpd | ✅ Clean (round 74, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=3 changed=1 failed=0` cold, `ok=3 changed=0 failed=0` warm - fully idempotent), `tftpd-hpa` service `active` on both. |
| robertdebock.xinetd | ✅ Clean (round 73, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=3 changed=1 failed=0` cold, `ok=3 changed=0 failed=0` warm - fully idempotent), `xinetd` service `active` on both. |
| robertdebock.go | ✅ Clean (round 72, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=8 changed=3 failed=0 skipped=5` cold, `changed=0` warm both - fully idempotent), `go version`/profile.d scripts byte-identical. One cosmetic-only warm-recap count diff (`unarchive:`'s `creates:` guard already short-circuits before any real work on both engines - only whether it's displayed as `skipping:` vs `ok:` differs). |
| robertdebock.git | ✅ Clean (round 71, 0.9.402; distinct from the already-tested `geerlingguy.git`). 1 real bug found and fixed: `getent:`'s single-key lookup branch returned a bare field-list instead of a dict keyed by the looked-up username (real Ansible's own `getent_passwd` is always `{"username": [...]}`, even for a single key) - broke the role's own `getent_passwd[git_username] != none` existence-check guard (shared by `robertdebock.users`), silently skipping every downstream task (git-config directory, `.gitconfig`, repo-destination directory, the actual `git clone`). Live-reverified: `ok=11 changed=4 failed=0 skipped=1` identical both engines, `.gitconfig` + cloned repo byte-identical, fully idempotent warm rerun. |
| robertdebock.diskspace | ✅ Clean (round 70, 0.9.399-0.9.401). 3 chained real bugs found and fixed: an outer include_tasks: loop's propagated "item" clobbered the inner loop's own item on every iteration (the role's disk-space check silently never fired at all); `ansible_facts['mounts']` was missing size/inode statistics entirely (new `gather_mount_space_stats`); and two independent Int32-narrowing overflow bugs (`JSON::Any#as_i`, Crinja's own `int` filter) silently produced wrong/default results for real byte-scale numbers. Live-reverified: `ok=12 changed=0 failed=0 skipped=6` identical both engines. |
| robertdebock.at | ✅ Clean (round 69, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=3 changed=1 failed=0` cold, `ok=3 changed=0 failed=0` warm - fully idempotent), `atd` service `active` on both. |
| robertdebock.cups | ⚠️ Real gap found, not fixed (round 68, no version bump). The pinned Galaxy release's own `template: validate: cupsd -t -c %s` (missing `-s /etc/cups/cups-files.conf`, already fixed upstream in newer releases) makes `cupsd -t`'s cups-files.conf lookup implicitly relative to the validated file's OWN directory. Real Ansible's `template:` renders its validate temp file under its own remote-tmp location (not `/etc/cups/`), so `cupsd -t` can't find `cups-files.conf` there and the task genuinely fails on real `ansible-playbook` too (confirmed deterministic, not a flake, via a second full run). crystal-ansible's own `template.cr` deliberately colocates its validate temp file with the destination directory (`/etc/cups/`, for a same-filesystem atomic rename) - `cupsd -t` finds `cups-files.conf` right there and validation (harmlessly) succeeds, so crystal completes the role cleanly where real Ansible fails. A genuine template-temp-file-location divergence, not a quick fix (relocating it to match real Ansible's remote-tmp risks breaking the atomic `File.rename` across filesystems for every OTHER `template:`/`validate:` use) - documented rather than same-session-patched, same precedent as round 52/62. `cups` service functionally healthy on the crystal host (`active`, HTTP 200 on :631) regardless. |
| robertdebock.autofs | ✅ Clean (round 67, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=14 changed=4 failed=0 skipped=6` cold, `ok=13 changed=0 failed=0 skipped=6` warm - fully idempotent), byte-identical `/etc/auto.master`/`auto.master.d`/`auto.home`, `autofs` service `active` on both. |
| robertdebock.mount | ✅ Clean (round 66, 0.9.397-0.9.398). 2 real bugs found and fixed: the "undefined" sentinel string wasn't treated as falsy in a bare filter-chain `when:` condition (`mount_requests \| regex_search("swap")` always fired the "Run swapon" handler regardless of the real match), and `loop_control.index_var` was entirely unimplemented (`{{ idx }}` always "undefined", breaking the role's own `results[index].stat.exists` re-creation guard - crystal kept "fixing" an already-correct mountpoint dir on every rerun). Live-reverified: `ok=10 changed=2 failed=0 skipped=4` identical both engines, bind mount active and byte-identical, fully idempotent warm rerun matching exactly. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical, no regression. |
| robertdebock.locale | ✅ Clean (round 65, 0.9.396). `community.general.timezone` was entirely unimplemented (new `timezone.cr` plugin added, systemd/`timedatectl` backend only - matches every target this repo benchmarks against). Live-reverified: `ok=22 changed=3 failed=0` identical both engines, `timedatectl show -p Timezone --value` + locale config byte-identical, fully idempotent warm rerun. |
| robertdebock.spamassassin | ✅ Clean (round 64, 0.9.395). 1 real bug found and fixed: the dotted-access dispatch's Crinja-first delegation only fell back to the hand-rolled evaluator (which already correctly re-templates a variable whose own value is itself unrendered `{{ }}` text) on a Crinja *exception* - a quiet Crinja `nil` (attribute access on a still-templated raw string failing to Crinja's `Undefined`, not an exception) fell through to the literal string `"undefined"` instead. `spamassassin_service.name`/`.state` (a role default computed via a dict-index-with-`default()`-fallback chain) hit exactly this, breaking `systemctl enable`. Fixed to also fall back on a nil result. Live-reverified: `ok=9 changed=6 failed=0 skipped=1` identical both engines, `spamd` user/group + log dir ownership byte-identical, fully idempotent warm rerun. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical, no regression (an initial apt "404 not found" on the first cold attempt was a transient stale-mirror issue, resolved by `apt-get update`, not an engine bug). |
| robertdebock.hostname | ✅ Clean (round 63, no version bump - zero crystal-ansible bugs found). `hostname_reboot: false` set to avoid the role's own reboot handler mid-round. Identical on both engines (`ok=6 changed=1 failed=0 skipped=1` cold, `ok=5 changed=0 failed=0` warm - fully idempotent), `hostname` matching exactly on both. |
| robertdebock.podman | ⚠️ Role itself broken on Debian/Ubuntu (round 62, no version bump) - `podman_service` (used by the "Restart podman" handler) is never defined anywhere in the role's `vars/main.yml`/platform vars files for Debian/Ubuntu, only presumably for EL/Fedora - `ok=8 changed=2 failed=1 skipped=2` identical on both engines, both failing at the exact same handler task. Not a new engine bug, but the FAILURE MODE differs and is worth noting as more evidence for round 52's deferred strict-undefined gap: real Ansible cleanly refuses to run the task ("The task includes an option with an undefined variable.. 'podman_service' is undefined") before ever invoking the module, while crystal-ansible substitutes the literal string "undefined" and lets `systemctl restart undefined.service` fail downstream with a more confusing message. Deliberately not fixed tonight - same architectural scope as round 52. |
| robertdebock.snmpd | ✅ Clean (round 61, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=21 changed=3 failed=0 skipped=6` cold, `ok=20 changed=0 failed=0 skipped=6` warm - fully idempotent), byte-identical `/etc/snmp/snmpd.conf`, `snmpd` service `active` on both. |
| robertdebock.dovecot | ✅ Clean (round 60, 0.9.394). 1 real bug found and fixed: `find:` only ever recognized the plural `paths:` parameter, failing outright on the singular `path:` alias - a real, documented alias of real Ansible's own `find` module (`aliases: ['path', 'name']`), and the far more common spelling for a single-directory search; the role's own "Find users in /var/spool/mail" task uses exactly `path:`. Live-reverified: `ok=8 changed=3 failed=0 skipped=2` identical both engines, byte-identical config, `dovecot` `active`, fully idempotent warm rerun. |
| robertdebock.vsftpd | ✅ Clean (round 59, 0.9.393). 1 real bug found and fixed - in the vendored `weirdbricks/crinja` fork itself (tag `crystal-play-0.9.9`), not crystal-ansible proper: `vsftpd.conf.j2`'s own `\| ansible.builtin.ternary('YES', 'NO')` idiom (a filter referenced by its fully-qualified collection name, valid real-Ansible syntax) crashed the WHOLE template render ("Unexpected POINT") - the fork's filter-name grammar only ever expected a single bare identifier after `\|`. Fixed to resolve a dotted filter/test name to its bare trailing segment. Live-reverified: `vsftpd.conf` byte-identical, `vsftpd` `active`, fully idempotent warm rerun on both engines. |
| robertdebock.python_pip | ✅ Clean (round 58, 0.9.392). 1 real bug found and fixed: `ini_file:` started from an empty line array for a brand-new destination file, while real Ansible's own module force-seeds a single leading blank line before the first `[section]` header whenever the file starts empty - a silent byte-for-byte content divergence in every fresh `ini_file`-managed config, not just this role's `/etc/pip.conf`. Live-reverified: `pip.conf` byte-identical after the fix, fully idempotent warm rerun on both engines. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical `pip.conf`, fully idempotent; the role has since gained `meta/argument_specs.yml` upstream, adding one extra `ok` task real Ansible only runs on ansible-core >=2.11 (the apt-installed comparison host is 2.10.8) - a comparison-host-version artifact, not an engine gap. |
| robertdebock.sysctl | ✅ Clean (round 57, no version bump - zero crystal-ansible bugs found). Identical on both engines (`ok=5 changed=1 failed=0` cold, `ok=5 changed=0 failed=0` warm - fully idempotent), byte-identical `/etc/sysctl.conf`, live `sysctl` values matching. |
| robertdebock.openssh | ✅ Clean (round 56, no version bump - zero crystal-ansible bugs found). Task-for-task identical on both engines (`ok=61 changed=2 failed=0 skipped=3` cold real vs `skipped=2` crystal - pre-existing `community.general.seport` cosmetic gap; `ok=60 changed=0` warm both - fully idempotent), byte-identical `sshd_config`, `ssh` service `active` on both, no lockout. |
| robertdebock.elasticsearch | ❌ Not testable (round 55, no version bump) - the published Galaxy release (`1.1.6`) installs a package literally named `elasticsearch-oss`, discontinued by Elastic years ago (current GitHub `master` already fixed this to plain `elasticsearch`, but that fix was never re-released to Galaxy) - `apt` fails identically on both engines with `E: Unable to locate package elasticsearch-oss`, reproduced independent of crystal-ansible. |
| robertdebock.php (+ robertdebock.httpd dependency) | ✅ Clean (round 54, 0.9.391). 1 real bug found and fixed: the `comment` Jinja filter silently ignored the `style` argument (`comment('c')`/`'cblock'`/`'xml'`/`'erlang'`), always producing the `'plain'` `#`-commented shape regardless - `php.ini.j2`'s own `{{ ... \| comment('c') }}` header needed `//`, got `#` instead (silently wrong content, not a crash). Ported real Ansible's full style-selection algorithm from `ansible/plugins/filter/core.py`. Live-reverified: `/etc/php/8.1/cli/php.ini` byte-identical after the fix, fully idempotent warm rerun, `apache2` `active`, HTTP 200. Skip-count diff is the same pre-existing `seport`/`seboolean`/`apache2_module` cosmetic gap as `robertdebock.httpd` (round 51). |
| robertdebock.cron | ✅ Clean (round 53, no version bump - zero crystal-ansible bugs found). Task-for-task identical on both engines (`ok=13 changed=5 failed=0 skipped=3` cold, `ok=12 changed=0 failed=0 skipped=3` warm - fully idempotent), byte-identical crontab, `cron` service `active` on both. |
| robertdebock.postfix | ⚠️ Role itself broken against real `ansible-playbook` by default (round 52, no version bump) - `vars/main.yml`'s `_postfix_virtual_X_domains` vars use `(X is defined) \| ternary(X \| join(', '), '<None>')`, and Ansible's `ternary` filter eagerly evaluates BOTH arguments regardless of the condition, so any of several optional-and-commented-out-by-default vars (`postfix_virtual_mailbox_domains`, `postfix_virtual_alias_domains`, ...) crashes real Ansible with `AnsibleUndefinedVariable` - reproduced independent of crystal-ansible. **Real parity gap found**: crystal-ansible's `ternary` filter only evaluates the CHOSEN branch (lazy), so it completed the whole role cleanly where real Ansible failed outright - `postfix` `active`, `ok=33 changed=2 failed=0 skipped=69`. Not fixed - the underlying issue (the whole hand-rolled evaluator has no strict-raising "undefined" concept at all, unlike Crinja) is broader than one filter and deserves dedicated investigation; see `KNOWN_MISSING.md`'s round-52 entry. |
| robertdebock.httpd (+ ansible.posix, community.general) | ✅ Clean (round 51, 0.9.390). 1 real bug found and fixed: `RoleLoader#resolve_role_dir` never searched `~/.ansible/roles`/`ANSIBLE_ROLES_PATH` - the default install location for a plain (non-collection) `ansible-galaxy role install`, breaking any bare Galaxy-role reference from a cwd without a local `roles/` checkout ("Role not found"). Fixed with a `roles_paths` search list mirroring the existing `collections_paths` handling. Live-reverified on a fresh `G3.2GB` Atlantic.net pair: cold `ok=18 changed=4 failed=0 skipped=28` (real) vs `ok=18 changed=4 failed=0 skipped=25` (crystal) - skip-count gap is the pre-existing documented `seport`/`seboolean`/`apache2_module` cosmetic scope gap, not new. Both `apache2` `active`, HTTP 200, fully idempotent warm rerun (`changed=0` both). |
| geerlingguy.kubernetes (+ geerlingguy.containerd) | ✅ Clean (round 35, 0.9.382; first new role since round 33's nomad). Real 2-vCPU/4GB Atlantic.net pair (resized up from 1 vCPU mid-round - kubeadm's control-plane needs 2). 3 real engine bugs found and fixed, all surfaced by the role's own "Set the kubeadm join command globally." task (`loop: "{{ groups['all'] }}", delegate_to: "{{ item }}", delegate_facts: true`): the `groups` magic variable was entirely unpopulated (always "undefined"), a templated `delegate_to:` on a loop was resolved once before the loop bound `item` (crashed trying to SSH to a host literally named "undefined"), and `delegate_facts:` was entirely unimplemented (the delegated fact silently stayed on the executing host instead of the target). Also hit the same stale-`ansible_default_ipv4` Atlantic.net quirk documented for round 33's nomad (worked around by pinning `kubernetes_apiserver_advertise_address`, not an engine bug). Final: real Kubernetes control-plane node `Ready` with every system pod `Running` (etcd/apiserver/scheduler/controller-manager/coredns/kube-proxy/flannel) on both engines identically, idempotent warm rerun matching task-for-task. **Re-verified round 36 (same version, no new bugs)**: extended to a genuine 2-node cluster per engine (control-plane + worker, 4 hosts total, real `kubeadm join`) specifically to stress-test the round-35 `groups`/`delegate_to`/`delegate_facts` fixes under real cross-host orchestration (the single-node round never actually had a second host to delegate *to*). Also needed `kubernetes_join_command_extra_opts: "--ignore-preflight-errors=all"` pinned in the playbook - the role's own `node-setup.yml` never applies `kubernetes_ignore_preflight_errors` to `kubeadm join` (only to `init`), so a real preflight-fatal (`ip_forward` not yet 1, port 10250 transiently held by the un-joined kubelet) failed identically on real `ansible-playbook` too - a role gap, not an engine one. With that, both a real python-orchestrated and a real crystal-ansible-orchestrated 2-node cluster came up fully `Ready` (both nodes, all 10 system pods `Running`), idempotent warm rerun `changed=0` on both. See `KNOWN_MISSING.md`'s `0.9.382` entry. **Round 38 perf follow-up**: after round 37's `include_tasks:`/`block:` parallelism fix (`0.9.383`), a finer-grained `pexpect`-timed re-run on a fresh 2-node pair showed crystal-ansible now *faster* than real `ansible-playbook` on both cold (128.6s vs 196.7s) and warm (15.5s vs 45.1s) runs - not just closer, as round 37's own less-precise timing pass had estimated. No new bugs; correctness unchanged (`failed=0` both, idempotent warm rerun). See `KNOWN_MISSING.md`'s round-38 entry. |
| robertdebock.vault (+ robertdebock.hashicorp) | ✅ Clean (round 35, 0.9.382; same round as kubernetes above, same host pair). 1 more real bug: the `ansible_apparmor` fact was entirely unimplemented, so the role's own `when: ansible_apparmor.status == "enabled"` guard on its `aa-enforce` hardening task always evaluated false regardless of the host's real AppArmor state - real Ansible ran the task (this host has AppArmor active), this engine always silently skipped it. Fixed to match real Ansible's own `ApparmorFactCollector` exactly. Vault service verified `active` on both engines, idempotent (the role's own `aa-enforce` task is hardcoded `changed_when: true`, so `changed=1` every single rerun on both engines identically is by design, not a bug). See `KNOWN_MISSING.md`'s `0.9.382` entry. |
| robertdebock.rsyslog (+ robertdebock.bootstrap) | ✅ Clean (round 50, no version bump - zero crystal-ansible bugs found). Task-for-task identical on both engines, byte-identical `/etc/rsyslog.conf`, service `active` on both, fully idempotent warm rerun. |
| robertdebock.dnsmasq (+ robertdebock.bootstrap) | ⚠️ Engine clean (round 49, no version bump) but `dnsmasq` itself can't start on this cloud image regardless of engine - `systemd-resolved`'s own stub listener already owns `127.0.0.53:53` on stock Ubuntu 22.04, and `dnsmasq --test` confirms the rendered config is syntactically valid on its own (pure port conflict, not a config bug). Everything up to that point matched task-for-task between engines. |
| robertdebock.squid (+ robertdebock.bootstrap) | ✅ Clean (round 48, 0.9.389). 1 real bug found and fixed: the role's own `assert.yml` has `squid_cache_dir.split(" ")[0] in [...]` (a Python-style `.split(...)` method call followed by indexing, in a bare `assert: that:` condition) - `ConditionalEvaluator`'s expression-routing guard didn't recognize a method call anywhere except a leading `(`, so it fell through to a naive dotted-path splitter that couldn't resolve it at all, always failing regardless of the real value. Widened the guard to any `(` in the expression. Live-reverified: `ok=21 changed=5 failed=0` matching real Ansible exactly, byte-identical `squid.conf`, `squid -k parse` valid, service `active`, fully idempotent warm rerun on both. See `KNOWN_MISSING.md`'s round-48 entry. |
| robertdebock.memcached (+ robertdebock.bootstrap) | ✅ Clean (round 47, no version bump - zero crystal-ansible bugs found). Task-for-task identical on both engines (`ok=16 changed=3 failed=0 skipped=2` cold, `ok=15 changed=0 failed=0 skipped=2` warm - fully idempotent), byte-identical `/etc/memcached.conf`, service `active`/answering `stats` on both. |
| robertdebock.gitlab_runner (+ robertdebock.bootstrap) | ✅ Clean (round 46, no version bump - zero crystal-ansible bugs found). Task-for-task identical on both engines (`ok=20 changed=4 failed=0 skipped=4` cold, `ok=20 changed=0 failed=0 skipped=4` warm - fully idempotent), byte-identical rendered `config.toml`. `gitlab-runner` `active`/v16.3.1 on both (unregistered - no real GitLab instance available to test `gitlab_runner_registration_token:` against, matching the role's own optional-registration design). |
| robertdebock.jenkins (+ robertdebock.java) | ⚠️ Engine bug fixed (round 45, 0.9.388) but Jenkins itself never installed on either engine - blocked by its own apt repo's GPG signing key having expired (`[expired: 2026-03-26]`, confirmed via `gpg --show-keys` independent of Ansible). Real bug found and fixed along the way: `robertdebock.java`'s own `assert.yml` uses `java_version \| int is number`/`in [...]` - a filter-chain type test - which `ConditionalEvaluator` treated as a literal (never-matching) variable name, always failing regardless of the real value. Generic fix (any `X \| filter is <type>` pattern was broken). See `KNOWN_MISSING.md`'s round-45 entry. |
| robertdebock.mongodb (+ robertdebock.bootstrap) | ⚠️ Engine clean (round 44, no version bump) but not runnable on this cloud CPU regardless of engine. Task-for-task identical on both engines (`ok=18 changed=6 failed=0 skipped=2` both), MongoDB 6.0's own apt repo added and `mongodb-org` installed cleanly on both. `mongod` crash-dumps identically on both (`signal=ILL`/`core-dump`) - MongoDB 5.0+'s well-known AVX CPU requirement, confirmed absent from this Atlantic.net VM's CPU entirely (`lscpu` shows no `avx` flag). A host/CPU-capability limitation, not an engine issue. |
| robertdebock.postgres (+ robertdebock.bootstrap) | ✅ Clean (round 43, 0.9.387). 4 real bugs found and fixed in sequence: `postgresql_db`/`postgresql_user`/`mysql_db`/`mysql_user` never recognized real Ansible's deprecated `name:` aliases (`db:`/`user:` respectively - the role's own tasks use exactly these); `postgresql_connection.cr` always forced a TCP connection to "localhost" instead of defaulting to a Unix socket the way real libpq does (this host's `pg_hba.conf` gates TCP behind `ident`, sockets behind `peer` - real Ansible connected fine, this plugin didn't); `login_host:`/`login_port:`/`login_user:`/`login_unix_socket:` never recognized their own deprecated aliases (`host:`/`port:`/`login:`/`unix_socket:` - the role sets a non-default `port: 6543` via the alias); and `postgresql_user`'s role creation used `CREATE ROLE` (implies `NOLOGIN`) instead of real Ansible's own `CREATE USER` (implies `LOGIN`), leaving created users unable to log in at all. Live-reverified: `\du` output identical on both engines, `rolcanlogin` confirmed `t` via direct SQL. See `KNOWN_MISSING.md`'s round-43 entry. |
| robertdebock.consul (+ robertdebock.bootstrap) | ⚠️ Engine clean (round 42, no version bump) but not fully runnable on this cloud image regardless of engine. Task-for-task identical on both engines throughout (`ok=28 changed=1 failed=0 skipped=5` both, byte-identical rendered `consul.hcl`). `consul` itself fails to start on BOTH engines identically - Ubuntu 22.04's apt-repo `consul` package is v1.8.7 (old), and the role's own rendered config uses a `license_path` key and `ui_config` block format that version doesn't understand at all (`invalid config key` from `consul agent`'s own parser, confirmed independent of Ansible) - a role/package-version mismatch, not a crystal-ansible issue. See `KNOWN_MISSING.md`'s round-42 entry for two other false leads (license-gated start task, a systemd start-limit race) ruled out along the way. |
| robertdebock.haproxy (+ robertdebock.bootstrap) | ✅ Clean (round 41, 0.9.386). 1 real bug found and fixed - in the vendored `weirdbricks/crinja` fork itself (tag `crystal-play-0.9.8`), not crystal-ansible proper: the role's own `haproxy.cfg.j2` template's `server.address | default(hostvars[server.name][...])` idiom crashed the WHOLE template render (not just that one lookup) whenever the `default()` fallback branch's own `hostvars[...]` chain hit a non-inventory host name, even though the fallback was never actually needed (`server.address` was already defined). Fixed to match real Ansible's own `Marker` class semantics (chained access on undefined self-propagates instead of raising; only an actually-*used* undefined value fails). Live-reverified: `haproxy.cfg` byte-identical, `haproxy -c` valid, service `active` on both engines, fully idempotent rerun. `community.general.seport`/`ansible.posix.seboolean` remain unimplemented (pre-existing scope gap, functionally identical skip on both engines on non-SELinux Ubuntu, just missing crystal-ansible's own `skipping:` banner). See `KNOWN_MISSING.md`'s round-41 entry. Round 134 (no version bump): re-ran cold+warm on a fresh pair, still byte-identical `haproxy.cfg`, valid, active, fully idempotent, no regression. |
| robertdebock.redis (+ robertdebock.bootstrap) | ✅ Clean (round 40, 0.9.385; first new role since round 35's vault/kubernetes). 1 real bug found and fixed on a fresh `G3.2GB` Atlantic.net pair: `mode: "{{ redis_mode }}"` (a templated string "640", no leading zero) got parsed as DECIMAL 640 by `template.cr`'s (and `copy.cr`'s/`ini_file.cr`'s) shared `starts_with?("0") ? octal : decimal` mode logic, producing octal `1200` (`--w------T`) instead of `0640` - `redis-server` couldn't even read its own config and crash-looped, while real `ansible-playbook` (which always parses an all-digit mode string as octal) came up clean. Fixed to match `file.cr`'s already-correct parsing. Live-reverified: `redis.conf` byte-identical content+mode on both engines, `redis-server` `active`/`PONG` on both, fully idempotent rerun (`ok=12 changed=0 failed=0 skipped=2` both). See `KNOWN_MISSING.md`'s round-40 entry. |
| devsec.hardening.nginx_hardening | ✅ Clean (round 39, no version bump - zero crystal-ansible bugs found). Fresh `G3.2GB` Atlantic.net pair, `geerlingguy.nginx` + `devsec.hardening.nginx_hardening`. Task-for-task match (cosmetic-only banner/`Gathering Facts` count diff, the usual artifact). Rerun showed `changed=9` (not idempotent) identically on **both** engines - traced to the hardening role's own `lineinfile`-style `nginx.conf` edits perpetually drifting against `geerlingguy.nginx`'s templated copy of the same file (a role-interaction quirk between two independently-authored roles, reproduced on real `ansible-playbook` too, not an engine bug), plus the two always-firing reload/restart handlers that follow. `nginx` `active` on both, config `nginx -t` valid on both (no vhost defined in this test playbook, so no `listen` socket on either engine - identical, not a divergence). |
| devsec.hardening.apache_hardening | ❌ Not testable - the published `devsec.hardening` Galaxy collection ships this role's directory completely empty (`tasks/`, `meta/` etc. all missing). It's a git submodule (`dev-sec/ansible-apache-hardening`) that the collection's release tarball never checks out. Reproduces identically on real `ansible-playbook` - the role silently contributes zero tasks and no error, not a crystal-ansible issue. |
| dev-sec.os_hardening (standalone role, `dev-sec/ansible-os-hardening`) | ✅ Clean as of 0.9.339 - see `git log`/prior round detail for that verification. Not the same code as the collection-form entry below; not re-tested since. |
| devsec.hardening.os_hardening (collection form, `dev-sec/ansible-collection-hardening`) | ✅ Fixed (0.9.487-0.9.488). Heaviest, most templating-dense role tested to date (100+ tasks). Two real bugs, found in sequence chasing a real-role performance benchmark: (1) a bare (non-`{{ }}`) `when: not lookup('varnames', ...)` condition never actually invoked the `lookup()` call at all, always evaluating the condition text as an undefined variable name - left `auditd_package` (and every other OS-family variable this role loads via a dynamic `set_fact:` loop) undefined, failing the role's very first real task; (2) task-level `vars:` were rendered unconditionally, before `when:` was even checked, so a `vars:` expression that legitimately raises (`| first` on a genuinely empty sequence, even with `| default(None)` right after it) crashed a task even when `when:` would have skipped it before real Ansible's own lazy per-key Jinja templating ever touched it - the role's mount-hardening task hits this for every disabled-by-default mount entry (6 of 9). Fixed with a narrow per-key rescue in `render_task_vars` (leaves the one failing key absent/undefined rather than crashing the whole task) rather than the broader lazy-Undefined redesign a first attempt at the filter layer needed and was reverted for (see `git log` `0.9.487`/`0.9.488`). Live-reverified on a real 2-host Atlantic.net `G3.2GB` pair (USEAST1, `--release`): `ok=102 changed=36 failed=0 skipped=50` cold (63.3s) vs real Python `ansible-playbook`'s `ok=101 changed=36 failed=0 skipped=50` (210.3s) - `ok` off-by-one is the already-documented `Gathering Facts` counting cosmetic; `changed=0` idempotent warm rerun on crystal (33.8s vs Python's 180.1s), real state verified on both hosts (`auditd`/sysctl hardening/`/etc/passwd` permissions), not just the recap. One residual, unexplained-not-investigated discrepancy noted rather than chased: `auditd` came up `active` on the crystal-side host but `inactive` on the python-side host post-run - plausibly the real, well-known "auditd needs a reboot to fully take effect" Linux quirk hitting one host's timing rather than an engine difference, but not confirmed either way; flag for a future round if it recurs. |
| konstruktoid.hardening | ❌ Not testable — role-side UFW lockout, reproduced in round 24. The role's `tasks/ufw.yml` enables the `ufw` systemd service (with its default `deny incoming` policy) **before** the `community.general.ufw` allow-rules tasks run, and a subsequent `Configure conntrack sysctl` block in `tasks/ufw.yml` reloads `net.netfilter.*` sysctls that flush the controller's persistent SSH connection through conntrack. Symptom: all new TCP connections (22, 80, anything) time out while ICMP keeps responding — textbook dropped-new-conns-after-conntrack-flush. Reproduced on a fresh 2-node `G3.2GB` Atlantic.net pair running real `ansible-playbook` (ansible-core 2.19.4 + `konstruktoid.hardening` v4.6.0 + `community.general` 12.5.0 from the role's pinned `requirements.yml`); lockout happened at ~2:24 elapsed / ~1:38 of useful work (the `Block blacklisted kernel modules` task in `tasks/kernelmodules.yml` was the last thing seen in the log before the controller's SSH went silent). **Both engines hit this identically** — the UFW lockout is in the role itself, not in any engine. The role's GitHub README explicitly says "the code is *not* idempotent and no longer actively maintained" and points users at the new `konstruktoid/ansible-collection-hardening` collection form; if we ever re-visit, the collection form is the more useful target, but until then the standalone role is a known-unrunnable environment, not a crystal-ansible gap. See KNOWN_MISSING.md's round-24 entry. |
| linux-system-roles (various) | ✅ Clean |
| devsec.hardening.mysql_hardening (collection) | ✅ Clean cold pass (round 24 role 2, 0.9.348). The modern collection form of the dev-sec hardening family (FQCN `devsec.hardening`, v10.6.0 - the standalone `konstruktoid.hardening` role redirects here; the standalone `os_hardening` does NOT redirect and is still maintained as a separate role at `dev-sec/ansible-os-hardening`). Cold on a fresh 2-node `G3.2GB` Atlantic.net Ubuntu 22.04 pair (ansible-core 2.19.4 vs crystal-ansible HEAD): crystal `ok=22 changed=3 failed=0 skipped=6` vs python baseline `ok=22 changed=8 failed=0 skipped=6` - the ok/skipped counts are identical; the changed count differs by 5 due to three deferred warm-idempotency bugs (file-plugin `chown`/`chmod` not honoring `follow: true` on symlinks, so `Protect my.cnf` lchown'd the symlink instead of chowning the target `/etc/mysql/mariadb.cnf` - leaving it at `root:root` instead of `root:mysql`; `mysql_user` reporting `changed: Updated user root` on every warm rerun when the password is already set; and the chmod side of the same symlink-following issue). Two real engine bugs fixed in this round: `role_loader.cr` `File.expand_path("~/.ansible/collections")` not expanding `~` (silently masking the default collections lookup for any CWD other than `$HOME` - same tilde-expansion bug class already fixed for `~/.my.cnf` in 0.9.346 and `BasePlugin#expand_tilde`), and `with_community.general.flattened` pushing the engine's "no value" sentinels (`"undefined"`, `""`, `"[]"`, `"{}"`) as one loop item (the role's "Ensure that there are no users without password" task tried `DROP USER undefined@%` on a fresh MariaDB). Also exercises the new `dict2items`/`items2dict` filters from 0.9.347 end-to-end via the role's own `mysql_hardening_options \| dict2items \| rejectattr('key', 'search', 'secure-auth') \| list \| items2dict` chain, working correctly in this live context. The three deferred warm-idempotency bugs (file-plugin `follow:` symlink handling, `mysql_user` `host_all:` idempotency) plus a `with_flattened` literal-source regression found while fixing them were addressed in `0.9.349`. Round 25 live-reverified all of it on a fresh `G3.2GB` pair plus found and fixed one more (`mysql_query` reporting `changed` on a 0-row DML statement) - warm rerun now `changed=0` on crystal, matching python's `changed=0` exactly (`0.9.350`, see `KNOWN_MISSING.md`). |
| geerlingguy.docker | ✅ Clean |
| geerlingguy.mysql | ✅ Clean |
| geerlingguy.postgresql | ✅ Clean |
| geerlingguy.nginx | ✅ Clean |
| geerlingguy.php | ✅ Clean |
| geerlingguy.security | ✅ Clean |
| openstack.ansible-hardening | ✅ Clean |
| githubixx.ansible_role_wireguard | ✅ Clean |
| ansible-community.ansible-vault | ✅ Clean |
| cloudalchemy.prometheus | ✅ Clean |
| cloudalchemy.grafana | ✅ Clean |
| geerlingguy.haproxy | ✅ Clean |
| geerlingguy.certbot | ✅ Clean |
| geerlingguy.redis | ✅ Clean |
| geerlingguy.postfix | ✅ Clean |
| geerlingguy.apache | ✅ Clean |
| geerlingguy.nodejs | ⚠️ Clean on the engine; final install step blocked by an external NodeSource/apt repo inconsistency (reproduces on real ansible-playbook too) |
| geerlingguy.jenkins | ✅ Clean |
| geerlingguy.elasticsearch | ✅ Clean |
| geerlingguy.memcached | ✅ Clean |
| geerlingguy.rabbitmq | ✅ Clean |
| geerlingguy.nfs | ✅ Clean |
| geerlingguy.ntp | ✅ Clean |
| geerlingguy.node_exporter | ✅ Clean |
| geerlingguy.firewall | ✅ Clean |
| geerlingguy.pip | ✅ Clean |
| geerlingguy.munin | ✅ Clean (after fixing `community.general.htpasswd`, previously unimplemented) |
| geerlingguy.samba | ✅ Clean |
| geerlingguy.supervisor | ✅ Clean (re-verified 0.9.253, byte-identical config, idempotent, `supervisorctl status` functionally authenticates with the `hash`-filter password) |
| geerlingguy.htpasswd | ✅ Clean (after fixing `community.general.htpasswd`, previously unimplemented) |
| geerlingguy.clamav | ✅ Clean (after fixing `.find(substring)` string method, missing from `VariableLookup`) |
| geerlingguy.kibana | ✅ Clean (re-verified 0.9.253, byte-identical config, idempotent) |
| geerlingguy.logstash | ✅ Clean (after fixing the `to_json` filter and `chdir=`/etc. with a templated, space-containing value) |
| geerlingguy.gitlab | ✅ Clean (after fixing handler `register:`/`changed_when:`/`failed_when:`, entirely unapplied, and the `bool` filter's keyword semantics) |
| geerlingguy.munin-node | ✅ Clean |
| geerlingguy.adminer | ✅ Clean (incl. its `geerlingguy.apache` config-writing path, functionally verified via `curl`) |
| geerlingguy.varnish | ❌ Not testable — role's own packagecloud.io apt repo has no valid Release file for Ubuntu jammy (reproduces on real ansible-playbook too) |
| geerlingguy.php-mysql | ❌ Not testable — role's own repo ships no `vars/Debian.yml` at all (reproduces on real ansible-playbook too) |
| geerlingguy.mongodb | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.consul | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.golang | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.registry | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.n8n | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.k3s | ❌ Not testable — role no longer exists on Ansible Galaxy |
| geerlingguy.puppet | ❌ Not testable — Puppet Labs' own published apt GPG key is expired (reproduces on real ansible-playbook too) |
| geerlingguy.phpmyadmin | ✅ Clean (after fixing two real engine bugs: `~/.my.cnf` option-file fallback entirely missing in `MysqlConnection.build_uri` — real Ansible's mysql_db defaults to reading `[client]` user/password from `~/.my.cnf`, crystal used none and failed on any login-less mysql_db task; and `lineinfile` (state=present) replaced only the FIRST regexp match instead of the LAST, matching real Ansible's lineinfile semantics — a `$cfg['Servers'][$i]['host'] = $dbserver;` template appears both as an active PHP-variable line and in a commented template near EOF, and real ansible rewrites only the final occurrence, leaving the active one in place. Both fixed with regression specs. Verified via a fresh 2-node Atlantic.net pair: cold crystal ok=80/changed=31/failed=0 vs baseline ok=96/changed=30/failed=0, warm `changed=1` on both (same `mysql_user` update_password re-assert), byte-identical `config.inc.php` + working `apache2.conf`, both serving HTTP 200 on port 8080. See KNOWN_MISSING.md `0.9.346` entry) |
| geerlingguy.git | ✅ Clean (incl. the full download/build-from-source path, not just the already-installed default) |
| geerlingguy.exim | ✅ Clean |
| geerlingguy.swap | ✅ Clean (after fixing `mount:`'s `name:` alias, missing `*`/`/`/`//` arithmetic entirely, and the `int` filter's native-float handling) |
| geerlingguy.tomcat6 | ❌ Not testable — the `tomcat6` package doesn't exist on Ubuntu 22.04 (only `tomcat9`); both engines also reject the role's own deprecated `state: installed` identically before ever reaching the package task |
| geerlingguy.mailhog | ✅ Clean (idempotent, service verified live via `curl`) |
| geerlingguy.filebeat | ✅ Clean (byte-identical config; `apt: update_cache: true`'s own always-`changed` quirk on rerun confirmed to match real ansible-playbook exactly, not a bug) |
| geerlingguy.ruby | ✅ Clean (after implementing `gem:`, entirely unimplemented, and fixing `apt:`'s idempotency for a purely virtual package name already satisfied via another package's `Provides:`) |
| geerlingguy.fluentd | ❌ Not testable — the role's own td-agent apt repo (packages.treasuredata.com) has no valid Release file for Ubuntu jammy (reproduces on real ansible-playbook too) |
| geerlingguy.composer | ✅ Clean (after fixing `get_url:`'s checksum algorithm silently using SHA1 for anything but md5/sha256, and `command:`/`shell:`/`unarchive:`'s `creates=`/`removes=`/`chdir=` not expanding a leading `~`) |
| geerlingguy.solr | ✅ Clean (after fixing 6 chained bugs: `creates=` extraction dropping single-template values, local-connection `become_user` plugin staging under a non-traversable install dir, Crinja missing `.split(...)` support entirely, Crinja's `trim_blocks` eating a literal space, and `file:` not chowning newly-created intermediate directory components — idempotent, service verified live via `systemctl`/`curl`) |
| geerlingguy.passenger | ❌ Not testable — the role's own apt-key fetch uses a stale key ID (`561F9B9CAC40B2F7`); the actual repo now signs with a different key (`D870AB033FB45BD1`) (reproduces on real ansible-playbook too) |
| geerlingguy.drupal | ❌ Not testable — its `composer require` task explicitly sets `become: false` (assumes a non-root deploy user); our benchmark harness connects as root throughout, so Composer's own root-safety check aborts regardless of engine (reproduces on real ansible-playbook too) |
| geerlingguy.java | ✅ Clean (idempotent, `java -version` functionally verified) |
| geerlingguy.containerd | ✅ Clean (idempotent, byte-identical `config.toml`, service verified live via `systemctl`/`ctr version`) |
| geerlingguy.helm | ✅ Clean (after fixing `not a or b` evaluating as `not(a or b)` instead of `(not a) or b` — real operator-precedence bug in the hand-rolled `when:` evaluator; idempotent, `helm version` functionally verified) |
| geerlingguy.gogs | ❌ Not testable — role version 1.4.3 uses the legacy `include:` directive, removed entirely from current ansible-core (real `ansible-playbook` refuses to parse the role at all), same failure mode as `geerlingguy.phpmyadmin` |
| geerlingguy.hdparm | ✅ Clean (idempotent, byte-identical `hdparm.conf`) |
| geerlingguy.daemonize | ✅ Clean (idempotent, correct version functionally verified; needs `build-essential` as a pre-task prerequisite on a bare image — real ansible-playbook needs it too) |
| geerlingguy.svn | ✅ Clean (after fixing `creates=`/`chdir=` extraction absorbing a second param's value; idempotent, repository created, `svnserve`/`apache2` verified live) |
| geerlingguy.blackfire | ✅ Clean (after fixing `apt_key:`'s idempotency for `url:`/`data:` with no `id:`, the common real-world shape; idempotent, packages + apache verified live) |
| geerlingguy.glusterfs | ✅ Clean, incl. a real 3-node cluster (own legacy `include:` patched locally to `include_tasks:`, and `glusterfs_ppa_version` overridden to `"9"` - the role's own default `"7"` has no jammy PPA release; both external, not crystal-ansible). Single-instance install clean on the first try. The 3-node cluster (real `ansible-playbook` orchestrating one cluster, `crystal-ansible` a separate one, both via real SSH, forming genuine replicated GlusterFS volumes) found two real engine bugs: `hostvars[<name>]` was entirely unimplemented, and `'x' not in y.stdout` mis-split when the literal itself contained the word "in". Both fixed; final state idempotent on both clusters, `gluster peer status`/`volume status` all green, cross-node file replication functionally verified through the mounted volume. |

| robertdebock.zabbix_server | ✅ Clean (after fixing 8 real engine bugs: Jinja2 `is boolean`/`is number`/`is string`/`is integer`/`is float`/`is iterable`/`is none` type tests entirely missing, the "undefined" sentinel not recognized by `default()`, no-arg `.split()` and a recursive-re-templating gap in dotted-access base fetch, `systemd:`'s `daemon_reexec:` unrecognized, `apt:`'s `deb:` param entirely unimplemented, `meta: flush_handlers` entirely unsupported - a real functional gap here, not cosmetic - plus a crash it exposed in the plugin pre-upload path for any `meta:` task against a real remote host, and a `mysql_user` idempotency bug in `update_password: always`; idempotent, `zabbix-server`/`mysql` verified live via `systemctl`+port checks on a fresh host) |
| robertdebock.zabbix_agent | ✅ Clean (same round/fixes as zabbix_server above; idempotent, `zabbix-agent` verified live via `systemctl`+port check) |
| robertdebock.grafana | ❌ Not testable - role does not exist on Ansible Galaxy or GitHub under this name (confirmed via the Galaxy API and a full listing of robertdebock's ~300 GitHub repos) |
| robertdebock.minio | ❌ Not testable - same (no Galaxy or GitHub role under this name) |
| robertdebock.bind | ❌ Not testable - same (no Galaxy or GitHub role under this name; robertdebock does publish `dns`/`dnsmasq`, but no `bind`) |
| robertdebock.postgresql | ❌ Not testable - same (no Galaxy or GitHub role under this exact name; robertdebock's closest role is `postgres`, a different name than requested) |

| robertdebock.nginx | ✅ Clean (zero engine bugs; idempotent, `curl` verified live) |
| robertdebock.mysql | ✅ Clean (after implementing `community.general.ini_file`, entirely missing, whose absence had also been silently swallowing robertdebock.mysql's own `Configure mysql server`/`client` tasks and cascading into a missed `Restart mysql server` handler; idempotent, `mysql -u root -p... -e 'select 1'` verified live) |
| robertdebock.docker_ce | ✅ Clean (zero engine bugs; idempotent, a real `docker run hello-world` verified live) |
| robertdebock.users | ✅ Clean (after fixing 3 real engine bugs: `include_tasks:`/`include_role:` with a scalar-template `loop:` never resolving at all - custom `loop_var` stayed "undefined" - plain-`{{ }}` `or`/`and` always coercing to literal "True"/"False" instead of real Jinja2 value-selector semantics, `getent:` never failing a missing single-key lookup - breaking a role's own `rescue:` block - and `password_hash` entirely unimplemented, silently storing plaintext into `/etc/shadow`; idempotent modulo `password_hash()`'s own real non-deterministic-salt property, confirmed identical on real ansible-playbook too) |
| robertdebock.phpmyadmin | ✅ Clean (after fixing 3 real engine bugs: `type_debug` filter entirely missing, `unarchive:`'s `extra_opts:` silently dropped - breaking `--strip-components=1`, the standard way to unpack a GitHub-release tarball - and the resulting tar-based idempotency check trusting `tar --compare`'s raw exit code instead of parsing its output the way real Ansible's own `TgzArchive#is_unarchived` does; idempotent, `curl` 200 on phpmyadmin's own `index.php` verified live) |
| Oefenweb.fail2ban | ✅ Clean (after implementing Python's `SEP.join(iterable)` string-method-call syntax, entirely missing - the reverse argument order of the `join` Jinja filter - plus one more recursive-re-templating copy in per-element list rendering; idempotent, `fail2ban-client status` showing the sshd jail verified live) |

| weareinteractive.nginx | ❌ Not testable - the role's own nginx.org apt-key setup fetches a now-invalid/stale GPG key (`NO_PUBKEY`, apt refuses the unsigned repo; reproduces on real ansible-playbook too) |
| weareinteractive.mysql | ❌ Not testable - role's own `tasks/main.yml` uses the legacy `include:` directive, removed entirely from current ansible-core (real `ansible-playbook` refuses to parse the role at all; same failure mode as `phpmyadmin`/`gogs`) |
| weareinteractive.redis | ❌ Not testable - same (legacy `include:`) |
| weareinteractive.users | ✅ Clean (after fixing 2 real engine bugs: `default(a ~ b ~ c)`, a Jinja2 `~`-concatenation `default()` argument, was unhandled - only `+`/`-` had the ExpressionEvaluator delegation - and `authorized_key:` wasn't idempotent for an empty `key:` value, a legitimate real-world case (no keys configured for a user); real Ansible's own module treats an empty key as a true no-op and doesn't even create the file, matched exactly. Also needed `ANSIBLE_ALLOW_BROKEN_CONDITIONALS=true` to get past a real ansible-core 2.19 strictness change unrelated to crystal-ansible, the role's own aging non-boolean `when:`. Idempotent, `id`-verified live) |
| Stouts.iptables | ❌ Not testable - same legacy `include:` issue as weareinteractive.mysql/redis (real ansible-playbook refuses to parse) |
| Stouts.timezone | ❌ Not testable - same |

| *(not a Galaxy role - targeted feature verification)* `docker_container`/`firewalld` | ✅ Clean (round 34, 0.9.378; first round since round 33's nomad). Not a role round - a custom playbook targeted at live-verifying `0.9.375`-`0.9.377`'s new `firewalld: port_forward:` and `docker_container: ports:`/`healthcheck:`/comparison-default work end to end via the real playbook-execution engine, on request, after those features had only been verified via direct plugin-binary invocation. Fresh `G3.2GB` Atlantic.net pair, real Docker Engine 29.1.3 (notably different from the Podman used to build the features) + firewalld installed fresh on both. Found and fixed 3 real bugs only reachable against genuine Docker Engine: two more `docr` fork nullability crashes (`Image#graph_driver`, alongside `ContainerInspectResponse#graph_driver`/`Health#log` found in the same session - Docker Engine 29.1.3 omits/nulls fields older/other-daemon inspect responses always populated) and a real daemon-API-version-dependent `HostIp` difference (`""` under this plugin's own unversioned/latest-API client vs `"0.0.0.0"` under real Ansible's own older pinned-API client) that silently recreated any container with `ports:` set on every single rerun - confirmed by live-patching the installed `community.docker` collection in place to dump its own actual/expected comparison values. Final result identical on both engines: `ok=20 changed=5 failed=0`, every assertion (idempotency, drift-detection, `comparisons: ignore`, container health, firewalld `port_forward` add/idempotent/remove) passed. See `KNOWN_MISSING.md`'s `0.9.378` entry. |
| robertdebock.nomad (+ robertdebock.hashicorp) | ⚠️ Engine clean (round 33, 0.9.363-0.9.365; first new role since round 32's nextcloud) but the role/Nomad-v2.0.5 combo can't fully succeed on this cloud host regardless of engine. 3 real crystal-ansible bugs found and fixed: `conditional_evaluator.cr` had no fallback for ANY unimplemented Jinja2 `is [not] <test>` (divisibleby, even, odd, equalto, sameas, etc) - always evaluated false regardless of the real outcome (fixed with a generic Crinja-delegation fallback); `apt_repository:`'s post-add `apt-get update` failure was silently swallowed instead of failing the task, breaking the role's own `block:`/`rescue:` GPG-key recovery pattern (fixed to propagate the failure, then fixed AGAIN to roll back the just-written repo line on that failure, avoiding a second-order "conflicting Signed-By" apt error from the rescue's retry). Cold pass `ok=26 changed=7 failed=1 skipped=13 rescued=1` vs python `ok=24 changed=0 failed=1 skipped=13 rescued=1`, task-by-task identical (cosmetic-only diff). Both engines hit the SAME expected external failure at "List server members" - Nomad's HTTP API doesn't bind 127.0.0.1 once `bind_addr` is a specific host IP (needed to work around a stale `ansible_default_ipv4` fact on this Atlantic.net host, itself needed because Nomad v2.0.5 won't start at all with the role's own default `0.0.0.0` bind_addr) - a real role/Nomad-version/CLI-default incompatibility, not an engine issue. See `KNOWN_MISSING.md` 0.9.363-0.9.365 for full detail. |
| robertdebock.nextcloud | ✅ Clean (round 32, 0.9.360-0.9.362; first new role since round 31's ssh_hardening). 3 real bugs found and fixed on a fresh `G3.2GB` Atlantic.net pair: `command:`'s `chdir:` crashed an otherwise-successful task under `become_user:` (tried to restore the process's original cwd afterward, which is `/root` on a fresh SSH connection and inaccessible to an unprivileged become_user - fixed by removing the pointless restore); `unarchive:`'s `owner:`/`group:`/`mode:` only applied to the destination directory itself instead of recursively (broke the role's own downstream `occ` commands needing the whole extracted tree web-server-owned - fixed with `chown -R`/`chgrp -R`/`chmod -R`); `package:`'s virtual/Provides:-satisfied package names (`php-dom`/`php-posix`, resolved via Provides: to `php8.1-xml`/`php8.1-common`, not real dpkg entries) always reported `changed` forever on warm reruns - same bug class already fixed in `apt.cr` for the ruby round but never ported to this separate `package:` code path, now ported. Cold pass `ok=27 changed=11 failed=0 skipped=6` vs python `ok=26 changed=11 failed=0 skipped=7` (cosmetic-only diff). Warm rerun fully idempotent on both engines (`changed=0`). Nextcloud verified reachable and functional (`curl` 200, correct www-data ownership recursively). See `KNOWN_MISSING.md` 0.9.360-0.9.362. Round 134 (no version bump): re-ran cold+warm on a fresh pair, `curl` 200 and ownership byte-identical on both, fully idempotent, no regression (an initial apache2 start failure was port 80 already held by an earlier `haproxy` role test reusing the same host pair - self-inflicted, not an engine bug). |
| devsec.hardening.ssh_hardening | ✅ Clean (round 31, 0.9.359; first new role since round 30's prometheus). 1 real bug found and fixed on a fresh `G3.2GB` Atlantic.net pair: `regex_replace`'s `\1`/`\2` backreference handling was backwards - it rewrote Python-style backreferences to `$1`/`$2` on the mistaken assumption Crystal's `gsub(Regex, String)` used Ruby-style `$`-backreferences (it doesn't; Crystal already matches Python's `\1` syntax directly), so the "translated" replacement was emitted completely literally. Broke the role's own `ssh -V` output parsing, cascading into 3 undefined openssh-version-gated `set_fact:` variables and a downstream loop crash. Cold pass `ok=34 changed=5 failed=0 skipped=5` vs python `ok=41 changed=8 failed=0 skipped=4` (task-by-task diff cosmetic-only). Both hosts locked out of root SSH identically post-play (role's own `ssh_permit_root_login: "no"` default, expected since we ran as root - not an engine bug, see `KNOWN_MISSING.md` 0.9.359 for detail). |
| prometheus.prometheus.prometheus | ✅ Clean (round 30, 0.9.358; first new role since round 28's pushgateway follow-up). 2 real bugs found and fixed on a fresh `G3.2GB` Atlantic.net pair: `fileglob`/`realpath` Jinja filters entirely missing from both evaluators (broke the role's own alert-rules-file-copy loop), and a Crinja fork parser bug where multi-arg parenthesized `is name(a, b)` TEST calls never split their arguments into separate positional args - broke the role's own `is version('2.7.0', '>=')` flag-selection guard, causing real prometheus to crash-loop on the deprecated `--storage.tsdb.retention` flag. Fixed in the `weirdbricks/crinja` fork itself (tag `crystal-play-0.9.7`), not just crystal-ansible. Cold pass `ok=38 changed=10 failed=0 skipped=14` vs python `ok=42 changed=14 failed=0 skipped=14` (count diff from scp-pre-staged binaries during investigation, task-by-task diff cosmetic-only). Idempotent, service verified live (`systemctl is-active` + `curl :9090/-/healthy` 200). See `KNOWN_MISSING.md` 0.9.358. |
| prometheus.prometheus.pushgateway | ✅ Clean (round 28, 0.9.356; `notify:` handler gap closed in 0.9.357 and live-reverified in a follow-up round on a fresh host pair). Round 28 found and fixed a real gap in 0.9.353's own task-name-rendering fix: round 26's eager include_role task-name substitution permanently baked in "undefined" for a task name referencing a var not explicitly passed via `include_role: vars:` (`_common_service_name`, computed internally) - removed entirely, fully superseded by 0.9.353's lazy render. 0.9.357 then fixed the deferred `notify:`-substitution gap (`_common`'s generic cross-role handler notification never firing) plus two related bugs found in the same investigation. **Live-reverified**: cold pass now correctly fires the restart handler with real output; a deliberate config-only-change warm rerun (forcing `pushgateway_web_telemetry_path`) confirmed the exact scenario the bug broke - the direct "enabled on boot" task reports `ok` (not `changed`, matching python), and the handler alone fires and restarts the service, verified via `systemctl status` (fresh restart timestamp) and `curl` against the new config path. See `KNOWN_MISSING.md` 0.9.356/0.9.357. |
| prometheus.prometheus.blackbox_exporter | ✅ Clean (round 27, 0.9.352). 3 real gaps found and fixed on a fresh `G3.2GB` Atlantic.net pair: `package.cr`'s `name:` parsing had the same Python-repr-list JSON gap as `apt.cr` (independent copy, actually the code path this role hits since it uses `ansible.builtin.package:`), and `community.general.capabilities` had no plugin at all (new `capabilities.cr`, ported from the real Python module's `getcap`/`setcap` parsing). Cold pass `ok=31 changed=9 failed=0 skipped=5` vs python `ok=34 changed=12 failed=0 skipped=7` (count diff is the known fact-cache artifact, not a real divergence - every task's status matched 1:1). `failed=0` on both, service verified `active` and answering `curl :9115`. Same cosmetic role-vars-task-name gap from round 26 observed again, not fixed. See `KNOWN_MISSING.md` 0.9.352. |
| prometheus.prometheus.alertmanager | ✅ Clean (round 26, 0.9.351). 7 real engine bugs found and fixed on a fresh `G3.2GB` Atlantic.net pair: dict-literal `== {}` comparison always false, `include_role: vars:` not re-rendering task names (also caused a real functional skip, not just cosmetic), `check_mode:`/`diff:` templated-value parse crash that silently dropped a whole task file (most severe - lost the task that writes alertmanager's own config), missing `version_compare`/`any`/`all`/`eq`-`lt`-`le`-`gt`-`ge`/`quote` Jinja tests and filters. Cold pass `ok=32 changed=8 failed=0 skipped=19` vs python `ok=35 changed=13 failed=0 skipped=20` (count diffs are a known fact-cache artifact plus a noted, non-blocking `copy:` re-upload inefficiency, not correctness issues) - `failed=0` on both, every expected task succeeded. One cosmetic-only gap left open (role vars/main.yml-sourced task names stay unrendered in banners). The role's own rendered systemd unit crash-loops real alertmanager 0.33.1 itself (`-cluster.listen-address=` empty-value CLI parsing bug) - reproduced independent of Ansible entirely, not an engine issue. See `KNOWN_MISSING.md` 0.9.351. Round 134 (`0.9.429`-`0.9.431`): 2 more real bugs found and fixed. `plugins/facts.cr` never set the flat `ansible_facts['python_version']` fact at all (missing since round 133's rewrite, even though it legitimately co-exists with the nested `ansible_python` dict in real Ansible) - the role's own `_common_dependencies` var does `ansible_facts['python_version'] is version('3', '<')` to pick `python-apt` vs `python3-apt`, and with the fact undefined the version test silently evaluated true, crashing the first real task by trying to install the nonexistent `python-apt` package. Also, `task_executor/executor.cr`'s single-host `run_task_list` printed a spurious empty `TASK [...]` banner for a named `block:` nested inside another `block:` (the role's own `_common`'s "Download binary {{ }}"/"Verify checksum of {{ }}" blocks) - blocks are transparent in real Ansible, only their members get banners; the multi-host path already had this guard, the single-host path didn't. Also fixed (same round) `unarchive.cr`'s `tar_changed?` unconditionally treating `tar --compare`'s Mode/Uid/Gid-differs lines as real changes even when the task's own `mode:`/`owner:`/`group:` explicitly overrides them - the role's own `mode: 0755` unarchive task was permanently non-idempotent (`changed: true` every rerun) because the archive's embedded member mode (0644) always differed from the requested 0755; fixed to match real Ansible's own `TgzArchive#is_unarchived` gating exactly. Live-reverified after each fix: `alertmanager` active, `curl` 200, recap `ok=34 changed=13 skipped=19` cold matching real Ansible's `changed=13`/`skipped=19` exactly, and fully idempotent warm rerun (`changed=0`) on both engines. |
| prometheus.prometheus.node_exporter | ✅ Clean (re-verified live AGAIN 2026-08-13, round 22 continued - after round 21's 16 bugs and round 22's first pass (6 more, closing out the Crinja convergence prep work), a THIRD live pass specifically to verify the step-5 dual-evaluator convergence (`or`/`and`/`is`, ternary, comparisons routing through Crinja first) found 7 more bugs (0.9.327-0.9.332), incl. a genuine CPU-pegging infinite-recursion hang (`prepare_crinja_vars` re-entrancy, not just a wrong value), `uri:`'s `dest:` file-writing entirely unimplemented, `.splitlines()`/`flatten`/`regex_findall`/`dict(iterable)` missing, `map('filtername', 'arg')` losing string-literal quoting on its inner call, a bare-identifier index-key (`dict[var]`) re-templating gap present in THREE independent evaluator copies, and `unarchive:`'s `extra_opts:` breaking on a templated (JSON-array-string) list value vs. a literal YAML list. Idempotent (`changed=0` on rerun), `node_exporter` service verified live via `systemctl`/`curl :9100/metrics`. See `KNOWN_MISSING.md`/`CRINJA.md` for full detail. |

| robertdebock.investigate | ⚠️ Engine clean (round 143, Ubuntu 22.04: ✅ clean, `changed=1` both engines) but failed identically on Rocky 9.6 on both engines - `screen` uninstallable without EPEL plus a pre-existing `curl`/`curl-minimal` package conflict on that specific Rocky 9.6 image, external/environmental. |
| robertdebock.grub | ✅ Clean (round 143, after fixing `lineinfile:`'s `mode:`/`owner:`/`group:` params being silently ignored whenever the line content was already correct - found on Rocky 9.6, `GRUB_TIMEOUT=5` already present but `mode: "0664"` never applied against the file's actual `0644`; Ubuntu 22.04 was clean without the fix. Idempotent, byte-identical `stat` mode on both engines after the fix.) |
| robertdebock.forensics | ✅ Clean (round 143, Ubuntu 22.04: `ok=90 changed=19 skipped=16 rescued=6` byte-identical both engines) |
| robertdebock.fips | ✅ Clean (round 143, Rocky 9.6: `ok=7 changed=1` byte-identical both engines) |
| robertdebock.vdo | ⚠️ Engine clean for the tested path (round 143, Rocky 9.6: `package:` install of `vdo`/`kmod-kvdo` itself - `ok=3 changed=1 skipped=3` byte-identical both engines) but `community.general.vdo` (the actual device-creation module) remains entirely unimplemented and untestable without a real block device (`vdo_devices` defaults to `[]`). |
| robertdebock.natrouter | ✅ Clean (round 143, after implementing `ansible.builtin.iptables`, entirely unimplemented - the role's only real task, a NAT MASQUERADE POSTROUTING rule, was previously silently dropped at parse time. New `plugins/iptables.cr` + a unit-tested `iptables_command.cr` rule-builder. Ubuntu 22.04: `iptables -t nat -L POSTROUTING -n` byte-identical both engines after the fix, idempotent.) |
| robertdebock.gnome | ✅ Clean (round 143, after fixing 2 real bugs in `package.cr`'s dnf backend: a single `@Group Name` (dnf comps-group syntax, e.g. Rocky 9.6's own `@Server with GUI` default) was silently mangled into 3 bogus space-split tokens, and even after that fix the group's own idempotency check used `rpm -q` which has no concept of a dnf group at all, always reporting `changed: true` forever. Fixed via a `single_name` tracking flag through the dnf/apt handlers plus a new `dnf group list installed`-based `dnf_group_installed?` check. Rocky 9.6 live-verified: cold `changed: true`, warm rerun `changed: false` matching real `ansible-playbook`'s own `ok=3 changed=0` exactly. Ubuntu 22.04 (`gnome-session`/`gdm3`, no `@group` syntax) was clean without the fix: `ok=3 changed=2 skipped=2` byte-identical both engines.) |

| robertdebock.glusterfs | ✅ Clean for the tested (single-node, no bricks/volumes configured) path (round 144, Ubuntu 22.04: `ok=6 changed=2 skipped=11` vs real Ansible's `ok=6 changed=2 skipped=12` - the 1-skip cosmetic difference is `gluster.gluster.gluster_volume` (unimplemented, unreachable `Configure volume` block) being dropped entirely at parse time instead of appearing as a "skipping" task; idempotent, `changed=0` on rerun.) |
| robertdebock.ara | ✅ Clean (round 144, after fixing `pip.cr`'s `already_installed?` not stripping a PEP 508 extras suffix - `pip: {name: "ara[server]"}` never converged to idempotent, `pip show 'ara[server]'` itself fails. Rocky 9.6: `ok=13 changed=5 skipped=13` byte-identical cold on both engines, `ok=11 changed=0` idempotent warm rerun on both after the fix. The ara web UI itself 500s on both engines identically - role doesn't run Django migrations, external role gap.) |

| robertdebock.kibana (+ .elastic_repo) | ✅ Clean (round 145, with `kibana_type: elastic` override - the role's own default `oss` fails identically on both engines since Elastic dropped `kibana-oss` packages years ago, external. `ok=15 changed=4 skipped=5` byte-identical cold both engines, service verified `active` via `systemctl`, idempotent warm rerun.) |
| robertdebock.ansible | ✅ Clean (round 145, after manually installing `python3-pip` on the fresh Rocky 9.6 image - both engines failed identically without it, external. `ok=7 changed=4 rescued=1` byte-identical cold both engines, `ansible.cfg` byte-identical, idempotent warm rerun modulo the role's own always-fails-then-rescues shape which legitimately reruns `rescued=1` every time on both engines.) |

| robertdebock.bareos_dir (+ .bareos_repository) | ⚠️ Engine clean for the reachable path (round 146, after fixing `ansible.builtin.debconf`, entirely unimplemented - `.bareos_dir`'s own "Prevent db installation (apt)" task, live-verified `debconf-show` byte-identical between engines, idempotent) but the role's own "Run database setup scripts" task fails identically on all 4 host/engine combos (Ubuntu 22.04 + Rocky 9.6) with `sudo: unknown user: postgres` - the role assumes PostgreSQL already has its system user set up, external/environmental, not a crystal-ansible bug. |

| geerlingguy.elasticsearch-curator | ✅ Clean (round 147, after fixing `pip.cr`'s `target_spec` not treating an empty-string `version:` as unpinned - the role's own `version: "{{ var | default(omit) }}"` with `var` defaulting to `''` built a literal `pkg==` spec, which pip rejects; real Ansible's own module uses Python truthiness and correctly treats it as unpinned. `ok=8 changed=5 skipped=2` cold, idempotent warm rerun on both engines.) |
| robertdebock.openbao (+ .openbao_server) | ✅ Clean (round 147, with a real working `openbao_server_config`/directories passed in - the role's own default leaves the daemon with no listener configured at all - and `openbao_version` overridden to `2.6.1` since the role's own default `2.5.0` is no longer in the upstream repo, confirmed failing identically on real Ansible first. `ok=18 changed=5 skipped=5` byte-identical cold on both engines.) |

**Legend:** ✅ Clean = engine matches real `ansible-playbook` (idempotent,
healthy services, config parity) as of the last time it was run. ⚠️ = engine
itself is fine, but the role hit an external/environmental blocker partway
through. ❌ = role could not be run at all (Galaxy removal or a bug in the
role itself, not crystal-ansible).
