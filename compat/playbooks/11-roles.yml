- name: roles
  hosts: localhost
  gather_facts: false
  roles:
    - role: greeter
      greeting_target: krikri-playbook
  tasks:
    - name: play's own task runs after roles
      ansible.builtin.copy:
        content: "play task ran\n"
        dest: /work/play-task-marker.txt
