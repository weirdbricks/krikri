# A non-local host (never actually connected to: the playbook below only
# runs controller-side action plugins, so no SSH is ever attempted) - this
# is the only way to reach execute_looped_task_batched, which
# loop_batch_eligible? excludes for local connections.
batchhost ansible_connection=ssh
