# Two distinctly-named local-connection hosts, for --limit regression
# coverage - inventory.ini's empty file only ever exercises the single
# implicit "localhost" path, which can't tell --limit apart from no
# --limit at all (there's only ever one host to match either way).
hostone ansible_connection=local
hosttwo ansible_connection=local
