# Minimal inventory for integration specs.
# Intentionally defines no hosts/groups: fixtures targeting "testservers"
# resolve to zero hosts and their play is skipped (not an error), while
# "localhost" is handled implicitly by the inventory parser.
