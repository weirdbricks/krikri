# Regression fixture: an explicitly-declared localhost with no
# ansible_user=, exercising the INI-parsed Host path (as opposed to
# inventory.ini's empty file, which takes the separate "implicit
# localhost" path in InventoryParser#get_hosts - always defaulting a
# non-nil user, and so never exercising Host#user staying nil).
localhost ansible_connection=local
